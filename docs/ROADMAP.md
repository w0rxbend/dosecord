# Dosecord ROADMAP

Status: adopted 2026-09-11 under [`docs/DESIGN.md`](DESIGN.md) (revision 2) and [ADR-001..ADR-013](adr/README.md). This is the single ordered plan for continuous development; it replaces the roadmap sections of `docs/PRODUCTION_PLAN.md` and `docs/SWARM_RESEARCH_SUMMARY.md`, which disagreed with each other and with the code (gap inventory, contradictions: "Roadmap ordering"). It is the reviewed draft of the same date with every accepted finding of the coverage, sequencing and experience reviews applied; Appendix C records the disposition of each finding.

## 1. Purpose and how to read this roadmap

This document tells a single developer (and any agent helping) what the next thin slice is, why it comes before the one after it, and what "done" means for it. Three orderings were merged and their conflicts are resolved here, once:

- **Product-first** sets the goal: the owner relies on Dosecord for their own Discord reminders as early as possible, and every milestone ends in something the owner uses. The Discord adapter therefore starts the moment the console proof is green and runs alongside the reminder engine, so the owner's daily mood logging moves to Discord before the first reminder is ever sent.
- **Risk-first** sets the constraint: any unknown that could invalidate the design is a time-boxed slice with a written go/no-go *before* the code that depends on it. Four such slices sit in M0 (tokens, DST, JDA DM, outbox idempotency); the Trixnity spike sits at the opening of the Matrix milestone because nothing before it depends on Matrix and the out-of-process escape hatch is designed and now sized (M6.3-alt). Three of the four M0 risk slices produce code that is kept; only the JDA spike is throwaway.
- **Foundation-first** sets the gate: the mediator, the pure occurrence engine, the loop and the dispatcher are test-gated and demonstrated on the console adapter before a vendor SDK is linked into the reminder path; the adapter conformance suite is the definition of "supports vendor X", and Discord is only the first adapter to pass it.

Resolutions that differ from at least one draft, including the reviewed one: documentation cut 1 is the first slice (M0.0), so every later slice amends documents that exist; Python and Kafka are deleted in M0.2 (ADR-008 amendment, section 4); the Discord adapter slices M2.1 and M2.2 depend only on M0 and interleave with M1, while the daily-driver release stays at the end of M2 because an untested reminder engine on a real regimen produces unreliable adherence data in a medical-adjacent product; the two-choice correction, a 7-day history, Pause/Resume, an interactive Today and Account -> Timezone are in the daily-driver scope, because a bot you trust with medication must let you check and repair yesterday's record from the chat; the Telegram adapter (M5.1) depends only on the Discord conformance gate and interleaves with M3, because ADR-005 and ADR-008 want the zero-core-files proof early, while cross-vendor fallback and linking (M5.2, M5.3) wait for the security review; the read-only REST API stays in v1.0 scope as the first candidate to defer at the M7 re-plan.

**Status vocabulary.** The review showed the gap inventory's `implemented` meant "code exists" even where the path could not run (R35/R63-R65 behind the DB-URL failure R70). This roadmap uses: `todo` (not started); `building` (a branch exists); `green` (merged to `main`, CI green under `-Werror`, every acceptance criterion an automated test or a recorded smoke transcript, docs updated); `in-use` (`green` plus the owner has used it on their real regimen for seven consecutive days with no P1 defect; required for slices marked with an asterisk in Appendix B); `blocked` (waiting on a named spike outcome); `deferred` / `dropped` (section 7; deferred has a re-entry condition, dropped does not). Appendix B is the only status source; Appendix A maps requirements to slices.

**Sizing.** S = one sitting, one PR. M = a few sittings, one PR. L = several sittings, two or three PRs, must name an intermediate green state. XL = must be split before it starts and appears only as a warning; no slice in this file is XL. No calendar dates and no day estimates anywhere, on purpose. Order is strict: a slice does not start until every dependency it lists is `green`; slices that list the same dependencies may interleave; "may interleave with M1" and similar notes say explicitly when a slice from a later milestone may run early.

**Requirement ids.** `R1..R86` are the gap inventory's requirements. `C1..C23` are the critic's `missing` items in file order. `K1..K13` are the critic's `corrections` in file order; those implying no work (K4 line numbers, K5 file count) say so. Every id appears in Appendix A mapped to a slice or to DEFERRED/DROPPED.

**Risk-first slices and spikes.** A risk-first slice is time-boxed and has a go/no-go rule; each writes `docs/spikes/<name>.md`, and a changed decision becomes an addendum section of the ADR it names. M0.6, M0.7 and M0.10 are risk-first slices whose code is kept and promoted (into `core/chat`, `core/domain` and `core/scheduling` respectively); M0.8 and M6.0 are spikes whose code is thrown away.

## 2. Where we are

**What exists.** A Python 3.10 monorepo of 36 files and 2,307 lines in three commits dated 2026-05-24 (gap inventory `architecture_actual`, K5): `shared/contracts` (a CloudEvents-style envelope, six command payloads, one chat response, a hand-maintained registry), a discord.py prefix-command bot with a fire-and-forget Kafka producer and optimistic canned replies, and a backend whose consumer, `CommandProcessor` and outbox publisher are wired to Kafka in ZooKeeper mode. The only path that works end to end is Discord DM to the `dosecord.commands` topic, and every command runs twice because the cog's `on_message` duplicates discord.py's own dispatch (R86). The backend cannot start as configured: every `DATABASE_URL` is `postgresql://` under `create_async_engine` with no `psycopg2` (R70). Nothing consumes `dosecord.chat.responses`, so every backend reply is produced into the void (R50).

**What is broken or missing.** The product core has zero implementation: no reminder worker (R22), no occurrence generation (R21), no dose actions or audit log (R25-R27), no missed handling (R23), no buttons, menus or wizard (R17, R24, R48), no stats (R31), no conversation state machine (R9). `actor.account_id` is trusted from the wire (R8). Timezone is hardcoded to UTC end to end (R10, R29). Every habit check-in creates a new habit (R39); every schedule create creates a new medication (R18). There are no migrations (R69), four contract tests and no CI (R80, R81), no DLQ, retry or flush (R66-R68), no structured logs or metrics (R74-R76). The critic adds: `reply_to` is ignored, the resolved account never propagates, duplicates get no reply, sessions have no conversation id, the `/` prefix collides with Discord's slash picker, and guild commands execute despite the DM-only rebuke (C1-C3, C5, C23, K1). Five planning documents contradict each other on naming, envelope fields, menu labels, topic layout and user ids; `ARCHITECTURE.md` and `DEVELOPMENT.md` actively teach retired patterns (R85).

**What is salvaged and what is deleted.** Salvaged, all language-neutral (gap inventory `salvageable`; ADR-008): the envelope and actor shape, the payload types with their defaults (offset 0, snooze [10,30,60], miss 120, max 3), the 13-table schema as the seed of Flyway V1 after corrections, the dedupe -> route -> record -> outbox transaction shape, the SKIP LOCKED publisher shape, `_parse_mood`/`_is_hhmm` and keyword maps as test fixtures, all user-facing and safety copy, the menu trees and callback payload shape, and `docs/MEDICATION_REMINDER_UX.md` as the product spec with ADR-012's answers appended. Deleted: the discord.py bot, the backend service, all Kafka producer/consumer/outbox-publisher code, Kafka/ZooKeeper/Kafka UI in compose, the three Poetry projects and PYTHONPATH hacks, the `models.py` shim, and the dev Postgres volume. Nothing is reused as executable code (ADR-001, ADR-008).

**What this commit lands.** `docs/DESIGN.md`, `docs/adr/ADR-001..013.md`, `docs/design-tracks/`, this file, the retired-document archive, reset stubs, and the documentation lint manifest complete M0.0's documentation cut. The executable prototype remains until the dependent M0.2 deletion slice.

## 3. Target

One Scala 3.8 direct-style process on JDK 25 virtual threads under Ox, built by Mill, on PostgreSQL 18 through pgjdbc/HikariCP with hand-written SQL and Flyway (ADR-001, ADR-002). Kafka is gone; the asynchronous edges are Postgres tables — `inbound_events` (idempotency with replayed reply), `outbox_messages` (at-least-once with lease, backoff, dead-letter, typed ops, vendor ownership), `domain_events` (append-only CloudEvents log) — plus LISTEN/NOTIFY as a wake-up (ADR-003).

The core exposes one vendor-neutral chat API through a `ChatMediator` (DESIGN.md section 4): an `Inbound` event enum, an `OutboundMessage` model (RichText AST plus ChoiceSet/Form/Notice blocks), a `ChatPort`. Adapters implement a small blocking `ChatAdapter` trait, declare a static `CapabilityProfile`, throw a closed `ChatError` hierarchy, and are Mill modules depending only on `contracts` and `core`; the module graph plus a CI grep make "adding a vendor touches zero core files" mechanical (ADR-005). A pure `Renderer` degrades every message deterministically through fixed ladders (buttons -> select -> numbered list with bot reactions -> numbered list; modal -> sequential `FormRunner`; ephemeral -> auto-delete -> notice). Every control carries a 49-char HMAC-signed `CallbackToken` that fits Telegram's 64 bytes and Discord's 100 chars and works on week-old messages (ADR-006). Acknowledgement happens before database work, driven by a static action registry (ADR-013).

Reminders are rows: append-only `schedule_revisions` with an exhaustive `Rule` enum are materialised 48 h ahead into `dose_occurrences` guarded by a cross-revision live-slot index; one loop claims by `next_action_at` with `FOR UPDATE SKIP LOCKED`, runs a pure exhaustive `decide()`, and writes the state change, `dose_actions` row, `domain_events` and outbox rows in one transaction with per-row savepoints and no network calls (ADR-004). Delivery is a vendor-owned outbox dispatcher with typed ops, epoch fencing and a bounded, flagged duplicate (ADR-009). Time is per-schedule IANA zone with `dst_kind` recorded on every row (ADR-011). Lifecycle semantics for quiet hours, snooze, undo, late log, chains and `unknown` versus `missed` are fixed (ADR-012). Identity is resolved by the core; production auth is a short-lived HTTPS link challenge with Argon2id, never a chat password (ADR-007). Matrix DMs are E2EE through Trixnity behind a Kotlin shim, gated by a spike (ADR-010).

Layout: `contracts/ core/ infra/ adapter-console/ adapter-discord/ adapter-telegram/ adapter-zulip/ adapter-matrix/ app/ tests/conformance/` (DESIGN.md section 3). Adapters are selected from `Settings` (`ENABLED_ADAPTERS`, one per present credential set), not by a CLI flag. Tests: munit + munit-scalacheck for domain and chat core, Testcontainers Postgres 18 for infra, a JSON adapter conformance suite in two halves (A: adapter against a fake wire; B1: renderer goldens for every profile; B2: mediator resolution per profile as each tier lands), a 30-virtual-day simulation (DESIGN.md section 11). Language-agnostic derivations: [`design-tracks/unified-chat-api.md`](design-tracks/unified-chat-api.md) (chat API), [`design-tracks/reminder-reliability.md`](design-tracks/reminder-reliability.md) (reminder reliability).

## 4. Milestones

### M0 — Reset, risk-first slices, and the console proof of the boundary

**Goal.** Documents that exist before code amends them; a green Mill repository with no Python and no Kafka; the full schema under Flyway; CI that blocks regressions; four architecture-invalidating unknowns answered in writing; and the mediator boundary proven by a console adapter running a real vertical.

**User-visible outcome.** `ENABLED_ADAPTERS=console dosecord run`: the owner creates an account by choosing a timezone, logs a mood, restarts the process, and the record and conversation step survive. The console remains the permanent boundary proof and the second channel for fallback tests; the owner's daily use moves to Discord as soon as M2.2 is green (section 4, M2).

**ADR-008 amendment (recorded in M0.2).** ADR-008 deletes the Python services in the same PR as the Discord adapter "so there is never a half state". The Python path cannot start (R70) and double-executes (R86), so there is no working state to protect; the deletion moves to M0.2. The ADR's consequence — the owner never maintains two stacks — is preserved.

Slices (strict order unless noted; M0.6-M0.8 are independent and may interleave after M0.1):

- **M0.0 Documentation cut 1** — S, `building`. Commit `docs/DESIGN.md`, `docs/adr/ADR-001..013.md` with `docs/adr/README.md`, `docs/design-tracks/`, this `ROADMAP.md` and an empty `docs/spikes/` (this commit); then, in the next PR: delete `docs/ARCHITECTURE.md` (teaches retired patterns); move `docs/SWARM_RESEARCH_SUMMARY.md` to `docs/archive/`; replace the obsolete sections of `docs/PRODUCTION_PLAN.md` with a pointer to DESIGN.md; mark `docs/API.md` planned with the M7.7 contract; reduce README, QUICKSTART and DEVELOPMENT to one-page "reset in progress; see docs/DESIGN.md and docs/ROADMAP.md" stubs that state the reset and keep no retired implementation guidance; write the doc-lint forbidden list and its allowlist (`docs/archive/`, `docs/adr/`, `docs/design-tracks/`, this file's section 2) as `docs/doc-lint.txt` for M0.4. Acceptance: the configured documentation scan returns no forbidden terms outside the allowlist; every ADR file has `Status: Accepted`. Covers R85 (part), C10. Deps: none.
- **M0.1 Mill build and module graph** — S. `build.mill` with `contracts`, `core`, `infra`, `adapter-console`, `app`, `tests/conformance`; `-Werror -Wunused:all -deprecation`; scalafmt, scalafix, munit, munit-scalacheck, Ox, upickle pinned to DESIGN.md section 2. Acceptance: `mill __.compile` and `mill __.test` green on an empty test; a throwaway test importing `infra` from `core` fails to compile (recorded in the PR). Covers R79, R82, R59, C9. Deps: M0.0.
- **M0.2 Delete Python, Kafka and the old compose stack** — S. Remove `services/`, `shared/`, Poetry files, Makefile Python targets, root `pyproject.toml`, Kafka/ZooKeeper/Kafka UI services and the old Dockerfiles; new `docker/compose.yml` with `postgres:18-alpine`, healthcheck, named volume; `.dockerignore`; append the ADR-008 amendment paragraph above as an addendum section of `docs/adr/ADR-008.md`; git history kept. Acceptance: `git ls-files '*.py' | wc -l` is 0; `docker compose up` reaches a healthy Postgres and nothing else; `git grep -il kafka` matches only `docs/adr/`, `docs/archive/`, `docs/design-tracks/` and this file. Covers R73, R77, R84, R62, C7, C8, C22, C10 (reset note). Deps: M0.1.
- **M0.3 Flyway V1, SQL layer, UnitOfWork, session repository, Testcontainers harness** — M. `V1__baseline.sql` = the enumerated table list: users, platform_identities, user_credentials, auth_challenges, recovery_codes, auth_audit_log, medications, medication_schedules, schedule_revisions, dose_occurrences, dose_actions, outbox_messages, rendered_messages, inbound_events, callback_slots, conversation_sessions (with `step_seq`/`version`), form_runs, delivery_channels, domain_events, worker_heartbeat, mood_checkins (with `tags text[]`), habits (unique `lower(name)`), habit_checkins, reminders, plus the `reminder_occurrences` view stub (dose rows only until M3.5); Postgres enums, reverse CHECKs, partial unique indexes, the cross-revision live-slot index, INSERT-only triggers on `dose_actions` and `auth_audit_log` whose one exception is `DELETE` while the session GUC `dosecord.erasure = 'on'` is set (the erasure path of M4.4; the app role cannot set it, a separate erasure role can), `timestamptz` everywhere; the ~150-line `sql"..."` interpolator and row mappers; HikariCP; `Clock` and `UnitOfWork` ports with the Postgres `UnitOfWork`; the `conversation_sessions` repository (`FOR UPDATE` load, versioned save). Rule: V1 is edited in place until the `0.1.0` tag (the dev volume is dropped on every change; no `V2` before M2 exit); expand/contract migrations apply from `0.1.0` on. Acceptance (Testcontainers Postgres 18): migrate then `validate` passes and is idempotent when run twice; tests reject an invalid status, `mood_level = 11`, a second live occurrence for one slot, `taken` without `taken_at`, and an `UPDATE` on `dose_actions`; a `DELETE` on `dose_actions` is rejected without the GUC and accepted with it under the erasure role; the interpolator round-trips `uuid`, `timestamptz`, `jsonb`, arrays; the session repository's versioned save rejects a stale version. Covers R69, R70 (superseded), R18 and R39 (uniqueness), R21 and R27 (tables), R29 (storage), C5, C11, K7. Deps: M0.2.
- **M0.4 CI and quality gates** — S. GitHub Actions: scalafmt check, scalafix, compile with `-Werror`, tests with a Postgres 18 service, `flyway validate`, vendor-symbol grep on `core/` and `contracts/` (`net.dv8tion`, `org.telegram`, `de.connect2x`, `kotlinx`, `sttp`), licence gate, doc-lint job from `docs/doc-lint.txt` with its allowlist, coverage-gate scaffold (85% on `core/domain` and `core/chat`, armed at M1 exit). Acceptance: a throwaway commit adding `import net.dv8tion` under `core/` is rejected; a PR with a failing test cannot merge; a throwaway commit adding the retired broker environment variable to README is rejected by doc-lint and the same string under `docs/archive/` is not. Covers R80 (foundation), R81, C21, R85 (lint). Deps: M0.3.
- **M0.5 Settings, composition root, health, image, shutdown** — M. `Settings` case class parsed from env and Docker secrets (`ENABLED_ADAPTERS`, `DATABASE_URL`, `CALLBACK_KEYS`, `ROLE`, `INSTANCE_ID`, `LOG_LEVEL`, `LOG_FORMAT`, `CONSOLE_USER_ID`, vendor credentials), `.env.example` generated by a Mill task and diff-checked; `dosecord run | migrate` with `--role gateway|worker|all` accepted (roles wired in M4.3); `dosecord migrate` under `pg_advisory_lock`; one Ox `supervised` root scope; SIGTERM ordering (stop inbound, stop claiming, finish in-flight within 20 s); Tapir netty-sync `/healthz`; multi-stage image (`eclipse-temurin:25-jre-alpine`, non-root, `-XX:+UseSerialGC -Xmx256m -XX:+AutoCreateSharedArchive`); the docker-build CI step added here, where the Dockerfile is born. Acceptance: committed `.env.example` equals the generated one; `docker compose up` yields `/healthz` 200; a SIGTERM test completes a fake in-flight fork and exits 0 within 20 s; two parallel `dosecord migrate` serialise on the lock; idle RSS with the console adapter enabled is <= 200 MB (recorded as a CI artefact and asserted). Covers R72 (entrypoints), R83, R78 (compose), R71 (superseded), C20, K6. Deps: M0.4.
- **M0.6 CallbackCodec and action registry (risk-first, code kept)** — S, time-boxed, go/no-go. `CallbackCodec` (34-byte layout, two active HMAC keys) and the action registry with `opensForm`/`visibility`/`requiresSession`, written in `core/chat` from the start. Properties: every valid token matches `^dc:[A-Za-z0-9_-]{46}$`, is at most 64 UTF-8 bytes and 100 chars, round-trips, any single-bit tamper is rejected under both key ids, old-key tokens verify during overlap. Go: all properties pass; the code and tests stay. No-go: MAC stays 8 bytes, inline value shrinks to 4 bytes and larger payloads are forced to slot mode; ADR-006 addendum. Note written to `docs/spikes/callback-tokens.md`. Covers R49 (callback shape). Deps: M0.1.
- **M0.7 DST materialiser primitives (risk-first, code kept)** — S, time-boxed, go/no-go. `resolveLocal` on `ZoneRules.getTransition` with `DstKind` in `core/domain`; golden rows *generated* from tzdb transitions for Europe/Kyiv, Europe/London, America/New_York, Australia/Lord_Howe, Asia/Kolkata, Pacific/Apia, America/Sao_Paulo. Go: Kyiv 2026-03-29 03:30 -> 04:30 local with `gap`; 2026-10-25 03:30 -> the earlier instant with `fold`; Lord_Howe 02:15 -> 02:45; JDK tzdb >= 2026c. No-go: the image adds a tzdata override step and the tzdb version becomes a startup assertion. Note written to `docs/spikes/dst-primitives.md`. Covers R29 (recurrence). Deps: M0.1.
- **M0.8 Spike C — JDA DM delivery and user-install context (throwaway)** — S, time-boxed, go/no-go. One slash command registered with `IntegrationType.USER_INSTALL` and the bot-DM context; a proactive DM from a user-installed app with no shared guild; `setNonce` with a fixture asserting `enforce_nonce: true` in the JSON body; button round trip; deferral under 1.5 s from the snowflake; modal on an un-acked interaction; slash option autocomplete round trip. Go: proactive DM works without a shared guild — the home-guild setup step is dropped and an ADR-001/ADR-013 addendum records that Dosecord registers `USER_INSTALL` only (DESIGN.md section 5 lists both install types). Partial: `canInitiateDm = SharedGuildOrInstall`, `GUILD_INSTALL` kept, home-guild step documented. No-go (DM interactions unreachable): reopen ADR-001's Discord-library lens before M2. Note written to `docs/spikes/jda-dm.md`; spike code deleted. Covers facts for R45, R47, R53, C23, K1, K13. Deps: M0.1.
- **M0.9 Contracts, chat model, renderer, FakeAdapter, golden files** — M. `contracts/`: `Envelope.scala`/`Actors.scala` transcribed with one `source` scheme (`dosecord.<module>`), sealed `Commands`/`Events` with the `.v1` suffix as the single version source, `Chat.scala` (`Inbound`, `InboundEvent`, `OutboundMessage`, `Block`, `CapabilityProfile`, `ChatError`, `ChatAdapter`, `CommandSpec` with `args`, `choices` and `autocomplete` flags), opaque ids, smart constructors (handle charset 3-80, `HH:MM`, bounded ints, no duplicate days), no JDK I/O imports (grep). `core/chat`: the promoted `CallbackCodec` and action registry from M0.6 with their property tests as part of this slice's acceptance; pure `Renderer` with every ladder of DESIGN.md section 4.3, five profiles, `FakeAdapter(profile)`, golden-file helper, discreet wrapping, text splitting, digest paging. Reaction/text-tier rules fixed here: multi-select `ChoiceSet` (`maxSelect > 1`) on rungs 3-4 renders "reply with the numbers, separated by spaces or commas" (reactions toggle, reply `done` submits); `finalize(handle, summary, keep = Some(choiceSet))` on the reaction tier posts the reply-quote with its own `choice_map` row and reaction set so post-Taken follow-ups stay one tap. Control layouts fixed here (with M1.4a's copy): reminder row 1 `[Taken][Snooze 10m][Skip]`, row 2 `[Snooze 30m][Snooze 1h]` filtered by bounds, styles Taken = success, Skip = secondary; post-Taken `[Undo][Correct]` (M3.1 adds `[Add note]`). Suite B1 (renderer goldens for all five profiles) is born here. Acceptance: upickle round-trip property for every type; a test asserting every `ChatAdapter` argument and return type derives `ReadWriter` (keeps ADR-010's out-of-process escape hatch honest); property: every `Envelope.source` matches `^dosecord\.[a-z]+$`; validation rejects `"noon"`, negative `missAfterMinutes`, duplicate days, `@handle`; a golden file per ladder rung per profile for the two-row reminder, a menu, a days-of-week multi-select, a post-Taken follow-up, a form and a 5,000-char body; discreet golden contains no medication name; `RenderReport` names the rung used. Covers R49, R55, R56, R59, R61, C6, C17, C1 (superseded: in-process reply). Deps: M0.6.
- **M0.10 Outbox dispatcher protocol (risk-first, code kept)** — M, time-boxed, go/no-go. Minimal dispatcher over `outbox_messages` in `core/scheduling` targeting the real `ChatAdapter` trait: short claim with 60 s lease and `attempted_at`, UNIQUE `send_key`, `FakeAdapter`, crash injection between the vendor call and the record write, two concurrent dispatchers on Testcontainers, `ops_done` resume of reaction sub-ops. Go: exactly one row per `send_key`; a crash after the fake send yields at most one extra send flagged `possible_duplicate` and no extra state change; lease expiry re-claims; the code is grown, not rewritten, in M1.7. No-go: redesign the claim/record protocol before M1.7. Note written to `docs/spikes/outbox-idempotency.md`. Covers R65, R67, R68 (proof). Deps: M0.3, M0.9.
- **M0.11 Telemetry skeleton** — S. scribe JSON with `ForkLocal` context (`correlation_id`, `vendor_event_id`, `account_id`, `vendor`, `handler`), scribe-slf4j2 bridge, masking processor redacting bodies and medication names above DEBUG, micrometer registry stub. Acceptance: a log-capture test shows `[redacted]` for a body logged at INFO; every line carries `correlation_id` when bound; `LOG_LEVEL` honoured. Covers R74 (core). Deps: M0.5.
- **M0.12a Mediator core on FakeAdapter** — M. `ChatMediator`: keyed executor (`vendor:chatId` striped over Ox channels), `inbound_events` insert-or-replay with the stored reply, `IdentityResolver` stamping `Principal` (no field for an account id from the wire), ack-policy scaffold from the action registry, the per-event transaction (state, `domain_events` with `type`, `source = dosecord.<module>`, `account_id = principal`, outbox rows, stored reply), bad-MAC audit row plus toast, `rendered_messages.choice_map` recording on send, resolution rules of DESIGN.md section 4.6 step 5 (quoted-reply numbers and reactions by target unconditionally; bare digits and hotkeys against the latest pending prompt only), the catalogue failure toast when the database is unavailable. Acceptance (munit, FakeAdapter, Testcontainers): the same `vendor_event_id` twice yields one row and the replayed reply; per-chat order preserved under interleaved Ox forks; an event without a stamped principal is rejected by the core; a tampered token yields the toast and an audit row and nothing reaches the core; every stored reply and every `domain_events` row carries the resolved `account_id`, and a handler test appends exactly one `domain_events` row with the expected `type` and `source`; a quoted reply "2" and a bare "2" resolve to the same token when the prompt is the latest, and only the quoted one resolves when it is not; a tap with the connection pool paused yields the catalogue failure toast and leaves no `inbound_events` row without `processed_at` (R68's "visibly fails"). Covers R8, R30 (primary), R50 (mediator half), R60 (primary: `inbound_events` PK), R64, R86, C1, C2, C3, C7 (correlation on rows), K2, K3 (section 1 vocabulary). Deps: M0.5, M0.9, M0.11.
- **M0.12b WizardEngine, sessions, sweeper, FormRunner** — M. `WizardEngine` over `conversation_sessions` (`FOR UPDATE`, `step_seq`, `version`, `updated_at` bumped on every step, persist-then-send, `last_prompt` re-send on restart), sweeper fork with "Still there? [Continue][Cancel]" at 30 min idle and abort after a further 30 min (slots deleted, prompt finalized "Setup cancelled — nothing saved"), `FormRunner` over `form_runs`, `/cancel` and `/menu` global interrupts, "Cancel the current setup?" on a new flow, direct tokens bypassing sessions, `[Back]`/`[Cancel]` on every step. Acceptance: kill mid-flow and restart resumes the same step; a stale `step_seq` tap gets a toast and the current prompt; `conversation_sessions.updated_at` advances on every step; advance 30 min -> "Still there?" prompt, +30 min -> session aborted and slots deleted; `FormRunner` on a text-only profile yields one `FormSubmitted` equal to the modal path's; a direct dose token mid-wizard is handled and the wizard is untouched. Covers R9, C5, K7. Deps: M0.12a.
- **M0.12c Console adapter** — S. `adapter-console`: stdin/stdout, numbered options, text-only profile, `CONSOLE_USER_ID` from Settings as the actor (so a second console identity exists for link tests), message numbers printed and a target syntax `#<n> <reply>` that produces `MessageReceived(replyTo = handle n)`, so quoted-reply resolution is exercisable from the console. Acceptance: the adapter passes the suite A scenarios applicable to a text-only profile (M0.13 formalises the runner; here the scenarios run as plain munit tests); `#3 1` resolves against message 3's `choice_map`. Covers R50 (adapter half), R58 (first adapter). Deps: M0.12a.
- **M0.12d First flows: account create, `/mood`, `/help`** — M. `ConversationStarted` -> Create (Link and Restore are hidden behind the feature flag until M4.1 and M5.3; never shown as "available later"); create asks for the timezone only: a `ChoiceSet(select)` of zones grouped by current offset with "Other" -> text, then "It is 14:32 for you now, right? [Yes][Change]"; the handle is generated and the display name left neutral (both editable under Account; a chosen handle is asked at the first Link or Export in M4.1), never copied from the vendor nickname; the create transaction inserts `users`, `platform_identities` and the `delivery_channels` row and appends `dosecord.identity.account_created.v1`; `/mood <1-10|word> [note] [#tag ...]` storing the note only when supplied, trailing `#tags` parsed into `tags text[]`, appending `dosecord.mood.checkin_recorded.v1`; `/help` rendered from the `CommandSpec` registry so it cannot omit a command. Acceptance: end-to-end munit transcript golden on FakeAdapter(console) and on the real console module: `/start` -> timezone pick -> confirm -> `/mood 8 slept well #sleep` -> one `mood_checkins` row with note `slept well` and `tags = {sleep}`; `/mood 8` stores `note = NULL` and `tags = {}`; an invalid zone re-shows the picker and writes no row; a second create from an already-linked identity replies with the catalogue error and writes no row; `users.display_name` is NULL and never equals the vendor `displayName` fixture; `domain_events` contains one `account_created.v1` and one `checkin_recorded.v1` with `account_id = principal`; `/help` lists every registered command including itself. Covers R1, R2 (create), R3 (already-linked), R10 (capture), R35, R37, R38, R51, C18, K8, K9. Deps: M0.12b, M0.12c.
- **M0.13 Adapter conformance suite v1** — M. `tests/conformance` JSON scenario runner (event mapping with stable `vendor_event_id`, duplicate -> same id, deadline auto-ack with fake clock, capability honesty, 429 -> `RateLimited`, "not modified"/duplicate-reaction -> success, delete-window and edit-not-found -> non-fatal `Permanent`, markup goldens, text split, resume, log hygiene, raw modal payload) with a `FakeVendorServer` interface — suite A; suite B2 (mediator resolution parameterised by profile: quoted reply, bare digit gate, stale step, ephemeral downgrade, ordering) for the console profile. Acceptance: the console adapter passes every applicable suite A scenario; a deliberately lying profile (claims `buttons`) fails capability honesty; the suite is a Mill task every `adapter-*` module must run. Covers R58 (gate), R80. Deps: M0.12d.

**Exit criteria.** Four spike notes with recorded go/no-go (M0.6, M0.7, M0.8, M0.10); CI is the only merge path; zero Python; the console transcript golden passes; every table of the enumerated V1 list exists and validates; M0.12d `green` and demonstrated by the owner on the console (the `in-use` week is measured on Discord, M2.2). **Non-goals.** Discord beyond the spike, reminders, medication features, web edge.

### M1 — Reminder engine, test-gated on the console

**Goal.** The whole dose lifecycle as pure, property-tested functions with the complete `decide` table, then real on Postgres — revisions, the loop, the dispatcher grown from M0.10, catch-up, the wizard, an interactive Today, history, and the one-tap and correction flows — all demonstrable on the console before any vendor SDK is linked into the reminder path. M2.1 and M2.2 (Discord gateway and components) may run in parallel with this milestone from M0 exit; they do not touch `core/`.

**User-visible outcome.** On the console: add a medication in three steps, receive "Time for Vitamin D, 1000 IU. 1) Taken 2) Snooze 10m 3) Skip 4) Snooze 30m 5) Snooze 1h" at the right local time, answer "1", see "Recorded at 09:03. 1) Undo 2) Correct", undo, log an as-needed dose, view Today and the last 7 days, pause and resume.

- **M1.1 Rule enum, ReminderPolicy, QuietHours** — S. `enum Rule` with every kind of DESIGN.md section 7.1 declared and exhaustive; only `FixedTimes` and `AsNeeded` accepted by validation until M7 (other cases return `Nil` behind a marker `-Werror` keeps visible); `ReminderPolicy` with ADR-012 defaults; `QuietHours` crossing midnight; validation (taper phases contiguous, inner rule not Taper/Chain, snooze options positive). Acceptance: adding a `Rule` case without a `match` arm fails compilation (documented negative test); property: any valid policy round-trips; invalid taper phases rejected. Covers R12 (enum), R16, R19 (types), C17. Deps: M0.9.
- **M1.2 Evaluator and materialiser (pure)** — M. `occurrences(revision, from, to)` for `FixedTimes` (any number of slot groups) and `AsNeeded` over local dates using M0.7's `resolveLocal`; `slot_key` scheme; 48 h horizon. Acceptance: seven-zone goldens from M0.7; properties: determinism, composability `occ(a,c) == occ(a,b) ++ occ(b,c)`, unique `(local_date, slot_key)`, wall-time round trip outside transition days, idempotent re-materialisation. Covers R13, R14 (rule), R21 (generation), R29. Deps: M1.1, M0.7.
- **M1.3 Occurrence FSM `decide()`** — L (intermediate green: system rows, then user rows). Pure exhaustive `decide(occ, policy, quiet, now, ctx)` over every `(status, event)` cell of ADR-012: due, repeat cadence bounded by `maxReminders`, quiet defer/silent/fallback with `reminder_deferred`, snooze wake, missed with delivery evidence, `unknown(outage|undelivered)`; taken with late-log window and `taken_late`, skipped with reason codes, snooze bounds and option filtering, correct with explicit `effective_at`, undo within 15 min, note; chain child creation declared but inert until M7.2; epoch increment; `Transition` carries action rows and dispatches. Acceptance: one named test per cell including `(missed, taken, now = +72 h)` -> correction flow, `(pending, tick inside quiet defer)` -> `reminder_deferred`, `(due, undo)` -> `due` with extended deadline, `(taken, taken)` -> semantic no-op, `(taken, undo, now = +20 min)` -> refused with the correction hint; properties: `unknown` never produced inside quiet hours; snooze options never pass the next occurrence; `reminder_seq <= maxReminders`; undo restores the prior projection. Covers R22 and R23 (semantics), R25, R26, R19, C13. Deps: M1.2.
- **M1.4a Copy catalogue and control layouts** — S. `core/domain/copy` with every reminder, follow-up, missed, undo, correction, digest, wizard, menu, ops-alert and help string, the forbidden-phrase list (clinical advice, shaming) and the recorded decisions: top menu `[Today][Medications][Habits][Reminders][Stats][Account]`, reminder rows per M0.9, missed notice `[I took it][Skip][Keep missed]`, post-Taken `[Undo][Correct]`, body "Time for Vitamin D, 1000 IU." with instructions on a second line when present, habit taxonomy boolean/count/duration/measurement plus avoid, ops alerts prefixed "Ops:". Acceptance: no catalogue entry contains a forbidden phrase; every `dose_action` kind has a sentence; the M0.9 goldens are regenerated from the catalogue and reviewed. Covers R46, R48 (labels), K12. Deps: M0.9.
- **M1.4b Projection fold and adherence** — M. `fold(actions, seed)`; adherence on `effective_at` with `unknown`/`cancelled`/manual excluded, on-time rate, median/p90 delay excluding corrections, today counts, streaks with neutral days. Acceptance: property `projection == fold(actions)` after any random action sequence; `0 <= adherence <= 1`; counts sum to resolved; `unknown` and `cancelled` never change adherence. Covers R32, R31 (medication maths). Deps: M1.3.
- **M1.5 Repositories, schedule revisions, materialiser job, fixture builder** — L (intermediate green: repositories and revisions, then the job). Occurrence, schedule, revision, action and outbox repositories over the M0.3 `UnitOfWork`; `schedule_revisions` append-only; create/pause/resume/archive/tz-change as revisions with `FOR UPDATE` on open rows, `effective_from` next-local-midnight rule, eastward-move rows kept and flagged for the M3.2 question, `tz_follows_user`; materialiser job with `ON CONFLICT DO NOTHING` (no target) and `materialized_through`; all queries take `now` as a bind parameter; a test-only `Fixtures` builder (account, medication, schedule, revision, occurrences at chosen instants) used by every later infra test; `dosecord.medication.schedule_created.v1` appended on create. Acceptance (Testcontainers): revision reconciliation cancels exactly the expected rows; pause -> advance 1 h -> resume fires the next slot; property: one live slot per `(schedule, local_date, slot_key)` across any sequence of edits, pauses and zone moves; two concurrent materialisers produce identical row sets; a create appends one `schedule_created.v1` row; CI grep for `now()` in SQL fails outside DEFAULT clauses. Covers R21, R11 (data), R15 (create/pause/resume/archive data side), R18, R10 (tz-follow), R30 (schedule_created). Deps: M1.2, M0.3.
- **M1.6 ReminderLoop** — L (intermediate green: claim and savepoints, then fencing and heartbeat). `tick` with `claimDue` (SKIP LOCKED, batch <= 50), per-row savepoints, quarantine at `error_count >= 3`, epoch fencing, `cancelOlderQueued`, `send_key` with epoch, per-instance heartbeat outside the batch, LISTEN `dosecord_wake`, safety-net materialisation; `dosecord.medication.dose_due.v1` appended on pending -> due. Acceptance: two concurrent loops produce exactly one outbox row per `send_key`; a poison row leaves the batch committed and the row quarantined with `next_action_at = now + 5 min`; tick-age metric exported; a pending -> due transition appends exactly one `dose_due.v1` row. Covers R22, R23, R66 (intent), R30 (dose_due). Deps: M1.3, M1.5.
- **M1.7 Outbox dispatcher and delivery channels** — L (intermediate green: ops and backoff, then fallback and buckets). Grown from M0.10: ops `send|edit|finalize|delete|react`, vendor ownership via advisory lock, `rendered_messages` + `choice_map` recorded before reactions, backoff 5 s..30 min with jitter, 8 attempts then `dead`, `possible_duplicate` after 2 min, `channelFatal` -> channel dead -> fallback after 5 min unsent, per-vendor/per-chat token buckets with priority; `delivery_channels`; `domain_events` consumed in-process after commit; the 30 s safety row for synchronously delivered replies; reminder text rendered from the M1.4a catalogue at send time. Acceptance: crash injection between send and record yields at most one extra message and no extra state change; a reaction-tier crash after the primary send leaves resolvable controls; a stale-epoch row is cancelled without a vendor call; property: the retry instants of a failing row follow 5 s..30 min with jitter within bounds and the ninth attempt is `dead`; `dead` increments `dosecord_outbox_dead_total`. Covers R65, R66 (dead status), R67, R68, R56 (facts), K10, K11. Deps: M1.6, M0.10, M1.4a.
- **M1.8 Catch-up and digest** — M. Materialiser-first startup inserting past-deadline rows as `unknown(outage)`; collapse-not-replay in `decide`; one digest per account per 15-min bucket with per-item epochs and a re-read before rendering; catch-up rate limit; `dosecord_unknown_total{reason}`. Acceptance: a 6 h clock jump yields `unknown` rows, one digest, no stale reminders; crash with a queued s1 row plus a 30 min jump yields exactly one sent message carrying the new epoch; the loop-equivalence property as restated in DESIGN.md section 7.5 holds on Postgres. Covers R22 (catch-up), R32 (`unknown` excluded), R67. Deps: M1.7.
- **M1.9 Add-medication wizard, main menu, Today, History, Pause/Resume, Account -> Timezone** — L (intermediate green: wizard and menu, then Today/History/Account). Declarative `Flow`: step 1 `Form(name, dose, times, instructions)` (times as `HH:MM` list, empty = as-needed; instructions optional) so Discord shows one modal and other vendors get four `FormRunner` questions; step 2 (when times given) days `ChoiceSet` `[Every day][Weekdays][Choose days...]` with the multi-select picker behind the last; step 3 one confirm card "Vitamin D 1000 IU, every day 09:00 Europe/Kyiv [Create][Change days][Change timezone][Cancel]"; `[Back]`/`[Cancel]` on every step and `/cancel`; reminder policy (At dose time / 5 min before / 10 min after, snooze options, miss window) keeps ADR-012 defaults and lives under Medications -> Settings; main menu per M1.4a; Medications submenu Today's doses / Add / Log dose / Pause-Resume toggle ("Paused since Tue [Resume]") / Settings / Edit (M3.2) / History, unshipped entries hidden by flag, never "under development"; find-or-create on `name_norm` so re-adding offers update; pause, resume and archive as revisions; `/today` interactive: per-dose `[Taken][Skip]` for pending/due rows and `[Log <name>]` per as-needed medication (within the 8 x 3 digest cap); `/history` read-only: last 7 days, per dose scheduled time, status, effective time, `taken_late`; Account -> Timezone re-revisioning `tz_follows_user` schedules with the echo "It is 14:32 for you now". Acceptance: wizard transcript golden through `FakeAdapter(console)` and `FakeAdapter(discord)`; the wizard survives a restart at every step (one test per step); `[Back]` from step 2 returns to a pre-filled form; `/cancel` mid-wizard leaves no medication row; instructions appear on the reminder second line and a skipped instructions field stores NULL with no second line; `dose_occurrences` rows exist for the next 48 h after confirm; pause -> resume from the menu fires the next slot; Account -> Timezone moves the next occurrence of a following schedule and leaves a non-following one; `/history` matches `dose_occurrences` for the seeded 7 days; `/help` lists every command including itself. Covers R10 (confirm, change), R11, R13, R15 (create/pause/resume/archive/today/history), R16 (exposed), R17, R18, R48, K9. Deps: M1.5, M1.4a, M0.12d.
- **M1.10 One-tap intake flows, correction, Log dose, universal commands** — L (intermediate green: Taken/Snooze/Skip/Undo, then correction and Log dose). Direct-mode handlers Taken / Snooze (options filtered) / Skip / Undo / Keep missed / I took it that `SELECT ... FOR UPDATE` the occurrence and call `decide`; the two-choice correction `dose.correct` -> "Log as taken: [Now][At scheduled time][Cancel]" reached from `[Correct]`, from a Taken after the 15-minute undo window ("The undo window has passed — correct it instead?") and from a direct Taken beyond the 24 h late-log window (Custom time, notes and reason chips arrive in M3.1); finalize on every recorded handle; repeat reminders finalize the previous message; missed notice; as-needed and manual logging through `/log <medication>` and Medications -> Log dose creating an `origin = manual` occurrence; universal commands `/taken [medication]`, `/snooze <min>`, `/skip [medication]`, `/log <medication>`, `/today`, `/history`; `dose_actions` rows with idempotency keys; `dosecord.medication.intake_taken.v1` appended on taken. Acceptance: console transcript golden per flow; a second Taken replies "Already recorded at 09:03" with no new action row; a `#<n> 1` reply on a 3-day-old reminder resolves by target and opens the correction (beyond the late-log window); a Taken 20 minutes after Undo's window offers Correct and records `manually_corrected` with an explicit `effective_at`; `/log vitamin d` creates one manual occurrence with `taken`; a tap on a row claimed by an in-flight tick waits and applies on the fresh row; every action row carries `idempotency_key`, vendor and message id; a taken appends exactly one `intake_taken.v1` row. Covers R24 (numbered tier), R25, R26 (taken/skip/snooze/undo/correct Now-Scheduled), R27, R14, R23 (missed notice), R30 (intake_taken). Deps: M1.7, M1.9, M1.4b.
- **M1.11 30-day scenario simulation** — M. Virtual clock through 30 days for the four personas available now (daily 09:00, twice daily, Mon/Wed/Fri, as-needed) with late answers, two snoozes, ignore, undo, correction, zone travel east and west, pause/resume, same-day edit, 6 h outage on day 12; dispatch transcript and final stats as golden files; personas needing M7 kinds registered as pending. Acceptance: goldens committed; any `decide` or loop change fails without a reviewed golden update. Covers R80. Deps: M1.8, M1.10.

**Exit criteria.** Coverage gate >= 85% on `core/domain` and `core/chat` enforced in CI; every FSM cell has a named test; all Testcontainers scenarios of DESIGN.md section 11.3 green; simulation goldens committed; the console demo transcript passes. **Non-goals.** Notes and custom-time corrections, schedule editing UI, stats, habits, non-MVP rule kinds, web edge.

### M2 — Discord daily driver

**Goal.** Put the tested engine behind Discord through the conformance gate, add the operations minimum (readiness, alerts, backups, operator CLI) and the docs cut-over, and deploy for the owner's real regimen. M2.1 and M2.2 start at M0 exit and run alongside M1; M2.3 onwards waits for M1.

**User-visible outcome.** A user-installed Discord app: `/start` with a timezone picker, a three-step wizard whose text step is one modal, an interactive `/today`, `/history`, reminders with `[Taken][Snooze 10m][Skip]` / `[Snooze 30m][Snooze 1h]` at the right local time, `[Undo][Correct]` after Taken, the missed notice, `/log` with autocomplete, a digest after downtime — all answering within the 3 s budget and mostly in one tap.

- **M2.1 JDA gateway, slash commands, ack policy** — L (intermediate green: event mapping before components). `adapter-discord`: JDA 6.6 login; global slash registration with `USER_INSTALL` (plus `GUILD_INSTALL` only if M0.8 was Partial) and bot-DM context only; slash options from `CommandSpec`: `/taken [medication]`, `/skip [medication]`, `/log <medication>` with autocomplete over `name_norm`, `/mood` with integer choices 1-10, `/snooze` with the filtered minute choices; no prefix commands, no message-content intent; guild-context interactions refused with an ephemeral notice as defence in depth (registration already makes them unreachable); `InteractionHandle` over `deferReply/deferEdit/reply/editOriginal/replyModal`; watchdog at 1.5 s from the snowflake, disabled for `opensForm`; every JDA event handed to an Ox fork; `AdapterLifecycle` on reconnect; `vendor_event_id = discord:interaction:{id} | discord:message:{id}`; `dm_channel_id` cache; `Unreachable` on 50007. Acceptance: conformance event mapping, deadline auto-ack (fake clock from `createdAt`) and resume green; an autocomplete request returns the seeded medication names; fixture: a guild invocation is impossible by registration; ack-latency histogram exists. Covers R45 (DM-only), R47, R53, R60 (discord ids), C23, K1, K13. Deps: M0.13, M0.8, M0.12d. May interleave with M1.
- **M2.2\* Components, modals, ephemeral, idempotent sends** — M. Buttons/selects/modals routed by the `dc:` prefix; `setNonce(base64url(sha256(sendKey)).take(22))`; `SUPPRESS_NOTIFICATIONS` for `silent`; spoiler for discreet; markdown renderer; ephemeral only on a live interaction, otherwise downgraded per ladder; `dose.correct` registered as `opensForm = false` (a button row), `dose.note` registered as `opensForm` but hidden by flag until M3.1 so no control exists without a handler. In-use week: the owner uses `/start`, `/mood` and `/help` daily on Discord (M0.12d flows) while M1 proceeds. Acceptance: fixture asserts `enforce_nonce: true` in the JSON body; a modal opened before a restart still submits; the account-create and `/mood` flows run end to end on a live DM; a text step opens a modal inside the budget with a fake clock; a missed deadline falls to `FormRunner` and is counted; conformance markup goldens green; the post-Taken reply golden for the discord profile renders exactly `[Undo][Correct]`. Covers R24 (buttons), R17 (modal steps), R49, R56. Deps: M2.1.
- **M2.3 Conformance gate, live smoke, tap budget** — M. Full suite A for the Discord profile; Discord profile in suite B2; the tap budget stated and checked: Taken/Skip/default snooze 1 tap, other snooze 1 tap, Undo 1 tap, Correct 2 taps, log an as-needed dose <= 2 taps; a documented live smoke checklist (fresh account without typing an IANA name, wizard on mobile, restart mid-wizard, reminder push received on Discord mobile within 10 s of due, Taken/Snooze 30m/Skip/Undo, Taken on a 3-day-old message replies with the correction prompt and `[Now][At scheduled time]` records `manually_corrected`, second Taken says "Already recorded", `/log` in two taps, pause -> resume from the menu, `/history` shows yesterday, digest after a forced 1 h outage). Acceptance: suites green in CI; checklist transcript attached to the release PR; `dosecord_interaction_ack_latency_seconds{vendor="discord"}` p95 < 1.5 s in the smoke run; a tap with the pool paused shows the failure toast on the live DM (R68). Covers R58 (gate), R86 (live). Deps: M2.2, M1.11.
- **M2.4 Readiness, core metrics, owner alerts, backups, operator CLI** — M. `/readyz` (DB ping, Flyway at head, tick age < 30 s, one adapter connected); `/metrics` with the core set (reminders due/sent/missed/unknown, outbox depth and dead, tick age, materialised lag, quarantined, ack latency, adapter connected, tzdb info); alerts DMed through the bot with `silent = true`, importance Info, an "Ops:" prefix, rate-limited to one per condition per hour, never interleaved between a reminder and its follow-up, and an optional separate alert recipient identity in Settings (tick age > 60 s, dead > 0, lag > 0, quarantined > 0, adapter disconnected > 5 min); compose adds `prodrigestivill/postgres-backup-local` (nightly `pg_dump -Fc`, 30-day rotation) and a restore drill script; `dosecord admin outbox ls|retry|dead`, `admin occurrence show|unquarantine`, and `admin identity link <user> <vendor> <vendor_user_id>` (operator-only bridge for the console-era account, no secret transits chat, audit row) — by default the Discord account is created fresh and console data is not carried. Acceptance: `/readyz` returns 503 when the loop is stopped; disabling the materialiser fires the lag alert in a test and a second trigger within the hour does not; `admin identity link` yields one `platform_identities` row and one audit row; `admin occurrence unquarantine` resets `error_count`; restore drill in CI (dump -> fresh container -> `validate` -> row counts match). Covers R76 (core), R78, R66 (operator side), R4 (operator bridge). Deps: M2.3.
- **M2.5 Documentation cut 2** — M. README, QUICKSTART and DEVELOPMENT rewritten for Mill, the module graph and the Discord setup; `docs/PRODUCTION_PLAN.md` reduced to the identity and observability sections DESIGN.md cites; ADR-012 answers appended to `docs/MEDICATION_REMINDER_UX.md`; contradictions settled in writing (namespace `dosecord.medication.*`, UUID `users.id`, the bot never confirms before the core). Acceptance: doc-lint green; a CI job follows README from clone to a running container and `/healthz` 200. Covers R85, C10. Deps: M2.3.

**Exit criteria (daily-driver release, tagged `0.1.0`, section 9).** M2.1-M2.4 `in-use` for seven consecutive days on the owner's real regimen, with every dose resolved from Discord. **Non-goals.** Notes and custom-time corrections, schedule editing, stats, web auth, second vendor, habits.

### M3 — Complete records and control

**Goal.** Every intake and every change is recorded, correctable and visible; schedules are editable without double-firing. M5.1 (Telegram adapter) may run alongside this milestone.

**User-visible outcome.** Add note, skip reasons, correct to a custom time, edit a schedule, quiet hours, discreet mode, `/stats`, paged history, a Sunday summary.

- **M3.1 Notes, skip reasons, custom-time corrections** — M. `dose.note` handler (modal on Discord, `FormRunner` on the console) and the `[Add note]` chip un-flagged so post-Taken becomes `[Undo][Correct][Add note]`; skip reason chips (forgot, ran out, side effects, clinician advised, other); Correct gains `[Custom]` (within `scheduled_for - 12 h .. now`) and a note field. Acceptance: corrections carry an explicit `effective_at` and are excluded from delay statistics; every action has a unique `idempotency_key`; transcript goldens on both profiles; the post-Taken golden renders three chips. Covers R25 (correction), R26 (reasons, custom), R27, R24 (`[Add note]`). Deps: M2.3.
- **M3.2 Schedule edit, timezone-move questions, Edit menu, History paging** — L (intermediate green: edit revisions, then the questions and paging). Edit entry reusing the wizard; the `effective_from` question ("today's 09:00 is already recorded; the new plan starts tomorrow"); the eastward-move question ("Keep tonight's dose at 21:00 New York?"); the non-following-schedule question on Account -> Timezone; archive finalizes open reminder messages; History paging the last 30 doses with Correct entry points; the instructions field editable. Acceptance: property — each slot exactly once in the 24 h after any sequence of edits, pauses and zone moves; the live-slot index rejects a duplicate under concurrency; history entries link to Correct; an eastward move asks and keeps the kept row. Covers R10 (change questions), R15 (update, history paging), R18, R29, R11 (edit). Deps: M3.1.
- **M3.3 Quiet hours and discreet mode** — M. `users.quiet_hours` with per-schedule override, modes deliver/defer/silent; discreet: `display_alias`, "your 09:00 dose", spoiler, preview suppression, ephemeral `[Details]`; Account settings entries. Acceptance: defer never produces `unknown`; a silent send carries the flag in the JDA fixture; the discreet golden contains no medication name; explicit snooze inside quiet hours is delivered silently. Covers R16 (discreet), R19, R45 (discreet). Deps: M2.3.
- **M3.4 Stats** — M. `/stats` and the Stats menu: today, 7/30-day adherence, per medication, on-time rate, median/p90 delay, streak with recovery copy; skipped shown separately, never as failure; in-process `domain_events` subscriber for stats invalidation. Acceptance: query result equals the pure `adherence` over the same rows (Testcontainers property); "no data" when the denominator is 0; stats golden over a seeded 30-day fixture with `unknown` and `cancelled` rows; a taken action invalidates the cached stats read through the subscriber (Testcontainers); forbidden phrases absent. Covers R31 (medication), R32, R30 (subscriber). Deps: M3.1, M1.4b.
- **M3.5 Generic reminder kind in the loop, weekly summary** — M. The `reminders` table consumer: `reminder_occurrences` view completed over `dose_occurrences` and `reminders` with `kind`, the loop claiming over the view, a minimal non-dose FSM (due -> done | snoozed | expired) with Done/Snooze controls, one `send_key` scheme, `next_due_at`; the Sunday evening summary as `kind = weekly_summary` through this path, silent flag, opt-out in Account. Acceptance: a `reminders` row with `next_due_at` in the past is claimed by the loop and produces exactly one outbox row; Done writes one action row with an `idempotency_key`; exactly one summary per account per week across restarts (`send_key`). Covers R33, R28 (infrastructure). Deps: M3.4.
- **M3.6 Simulation extended, nightly checks** — S. Personas gain notes, corrections, quiet hours and edits; nightly fold-vs-projection job and nightly tz verify pass (`system:tz_rules_changed`) with alerts. Acceptance: goldens updated under review; `fold-check` detects an injected drift; the tz pass rewrites zero rows on an unchanged JDK. Covers R80. Deps: M3.2, M3.3.

**Exit criteria.** M3.1-M3.4 `in-use`; the owner has corrected one dose to a custom time and edited one schedule on the real regimen; nightly checks green for seven days. **Non-goals.** Web edge, cross-vendor fallback, habits.

### M4 — Identity, security and operations

**Goal.** Cross-vendor linking without chat passwords, data rights, tracing and dashboards, and a security review — before cross-vendor delivery makes the link flow load-bearing.

**User-visible outcome.** Link and restore via an HTTPS page or an 8-char code, a chosen handle, recovery codes, doctor-ready export, account erasure, alerts and traces for the operator.

- **M4.1 Tapir web edge, auth-link, handle step, auth metrics** — L (intermediate green: challenge issue and page, then code path and metrics). `GET/POST /link/{token}` and `/restore/{token}` server-rendered with CSRF double-submit, generic errors, Postgres token bucket per IP and per challenge; `auth_challenges` kind setup|link|restore (SHA-256 token hash, 10-min TTL, single use, `created_ip`); the 8-char code path typed into an already-linked chat; the "choose a handle" step at the first Link or Export (charset 3-80, taken handle rejected); `AccountLinkCompleted` injected into both chats; `identity.platform_linked.v1` event; Link and Restore un-flagged on Discord; `dosecord_auth_failures_total{kind}` and `dosecord_ratelimit_hits_total{surface}`; Caddy in compose behind `PUBLIC_BASE_URL`. Acceptance: link a second identity end to end between two `FakeAdapter` vendors in munit and live Discord-to-console (`CONSOLE_USER_ID`); reuse, expiry and tamper yield the same generic page and an audit row each; 429 after the bucket and the metric increments; concurrent POSTs consume the challenge exactly once; a taken handle is rejected with the catalogue error and no row. Covers R4, R6 (challenge), R3 (taken handle), C4 (`platform_linked`), R76 (auth metrics). Deps: M2.4, M0.12d.
- **M4.2 Credentials, recovery, restore, unlink** — M. Argon2id via Bouncy Castle (`m=64MiB,t=3,p=1`), password set only on the web page, minimum length 12, no composition rules, rehash on parameter change, `failed_login_count`/`locked_until` read; hashed single-use recovery codes; restore flow; unlink with `unlinked_at`, confirmation and delivery-channel removal; sessions rekeyed on link; the `/start` menu shows Create / Link / Restore on every vendor from here. Passwords are never accepted in chat. Acceptance: Argon2 test vector; a recovery code works once; lockout after N failures; an unlinked identity receives no reminders and reaches only identity flows. Covers R5, R6 (Argon2id, recovery), R2 (full menu), C4 (unlink), C16 (non-deferred parts); R7 dropped. Deps: M4.1.
- **M4.3 Audit, key rotation, roles, remaining admin CLI** — S. `auth_audit_log` append-only; `dosecord admin tokens rotate` with two active key ids; `--role gateway|worker` wired with per-vendor advisory locks; `admin occurrence fold-check`. Acceptance: old-key tokens verify during overlap and yield a toast plus audit row after retirement; a second process claiming the same vendor refuses to start. Covers R6 (audit), R72 (roles). Deps: M4.1.
- **M4.4 Export and erasure** — M. `dosecord admin user export` (doctor-ready CSV/JSON: medications, revisions, occurrences with `effective_at`, actions, mood, habits) and `admin user delete` running under the erasure role with `SET LOCAL dosecord.erasure = 'on'` (the V1 trigger clause from M0.3), recorded as an ADR-002 addendum; Account -> Export sends a challenge-protected one-time download; Account -> Delete requires typed confirmation and a 7-day grace. Acceptance: export validates against a committed schema and contains no other account's rows; erasure leaves zero rows referencing the user except the audit tombstone; the same delete under the app role is rejected by the trigger. Covers R44. Deps: M4.1.
- **M4.5 Tracing, full metrics, dashboards** — M. OpenTelemetry Java agent plus manual spans (`mediator.handle`, `reminder.tick`, `outbox.dispatch`, `adapter.send`) with `traceparent` on outbox rows; remaining metrics (`form_degraded_total`, `adapter_errors_total`, `possible_duplicates_total`, and the M4.1 auth/rate-limit counters); `grafana/otel-lgtm` compose profile with a dashboard and alert rules. Acceptance: one reminder's trace spans materialise -> tick -> send; every metric named in DESIGN.md section 10 plus the two auth counters is scraped in a test. Covers R75, R76 (full), R78 (profiles). Deps: M2.4, M4.1.
- **M4.6 Security review gate** — S. Review of the link page, token codec, log hygiene and dependency tree against OWASP; findings fixed or filed as blocking; a dependency-audit CI step (known-vulnerability scan on the resolved tree, failing on high severity) as the automated half. Acceptance: review note in `docs/spikes/security-review-1.md` with zero open blocking findings; the audit step is green and a seeded vulnerable version in a throwaway branch fails it. Covers R6 (verification). Deps: M4.2, M4.4.

**Exit criteria.** Two identities on one account via the web link; restore drill performed by the owner on real data; recovery codes stored; security review passed. **Non-goals.** REST API, email, OAuth.

### M5 — Telegram and multi-channel delivery

**Goal.** Prove the mediator claim mechanically — a second vendor lands as one Mill module and one settings entry, touching zero files under `core/` or `contracts/` — as early as the Discord gate allows, and give the owner a fallback channel once linking is reviewed.

- **M5.1 TelegramBots long-poll adapter** — L (intermediate green: event mapping and sends, then keyboards and toasts). One poller per token, `update_id + 1` cursor, `InlineKeyboardMarkup` <= 4 per row with 49-char tokens, `answerCallbackQuery` exactly once with a pre-transaction toast (`transientAck = true, deferrable = false`), 2 s watchdog, HTML parse mode, `ForceReply` for text steps, `setMyCommands`, `disable_notification`, edits any age, `deleteOwn = Window(48h)`, 403 -> `Unreachable`, `RetryAfter` -> `RateLimited`, "message is not modified" -> success, `vendor_event_id = telegram:update:{id}`; single-vendor accounts only (a Telegram-only account via `/start` Create). Acceptance: CI diff gate — the PR touches nothing under `core/` or `contracts/`; suite A green including toast-after-auto-answer and the 2 s auto-answer; Telegram profile in suite B2. Covers R58, R60 (telegram ids). Deps: M2.3. May interleave with M3.
- **M5.2\* Multi-channel fallback proven** — M. `delivery_policy primary_then_fallback|broadcast`, `fallback_after`, dead-channel announcement on a healthy channel, post-resolution `finalize` fan-out ("Recorded via Telegram at 09:03"), Account -> Reminder channels menu. Acceptance: stopping the Discord adapter routes the reminder to Telegram within the fallback window; the first action wins and the other copy is finalized; a dead channel is announced once. Covers R58, R45 (DM-only on both). Deps: M5.1, M4.6.
- **M5.3 Cross-vendor link UX and live smoke** — S. `ConversationStarted` on Telegram presents Create / Link existing / Restore; link via URL or the code typed in Discord; live smoke checklist and setup guide. Acceptance: both chats receive the completion message; smoke transcript attached; doc-lint green on the guide. Covers R2 (full menu, second vendor), R4. Deps: M5.2, M4.1.

**Exit criteria.** Zero-core-files diff held; both vendors pass the shared suites; M5.2 `in-use` with Telegram as the owner's fallback for seven days. **Non-goals.** Mini Apps, reply keyboards, groups.

### M6 — Reaction tier: Matrix spike, Zulip, Matrix

**Goal.** Vendors without buttons get a real one-tap loop through bot-added reactions, and Matrix DMs are end-to-end encrypted from the first slice.

- **M6.0 Spike D — Trixnity from a Mill `kotlinlib` module, E2EE (throwaway)** — M, time-boxed to two sittings, go/no-go. Kotlin module implementing the Scala `ChatAdapter` trait, Trixnity 5.8.1 with the vodozemac driver, `runBlocking` on a virtual thread inside an Ox fork, encrypted DM round trip against a Synapse testcontainer, Exposed repository on Postgres schema `matrix` attempted, RSS and startup measured. Go: compiles in CI, E2EE round trip works, no carrier pinning or thread leak over 1,000 Ox cancellations, one repository option works. Partial: SQLite on a named volume in the backup job. No-go: M6.3-alt is built and M6.3 runs out of process. Note written to `docs/spikes/trixnity.md` with an ADR-010 addendum. Deps: M5.1.
- **M6.1 Reaction tier in the mediator** — M. What is new for reaction vendors only (choice_map, quoted-reply numbers and the digit gate exist since M0.12a): `ReactionChanged` resolved by target unconditionally, own-reaction filtering, finalize = remove own reactions plus reply-quote with `keep` carrying its own choice_map and reaction set, `controls_removed_at` -> "already recorded", reaction sub-ops within `eventsPerMessageBudget - 1` on a real adapter, multi-select via space/comma-separated numbers with `done`, digests as one numbered message with one reaction set. Acceptance: suite B2 for the zulip and matrix profiles; reaction 2 and reply "2" resolve to the same token; a reaction on a 3-day-old reminder resolves; crash after the primary send leaves resolvable controls; a days-picker reply "1, 3, 5" submits {Mon, Wed, Fri}; after finalize, Undo is one reaction on the reply-quote; a late reaction on a finalized reminder gets "already recorded". Covers R24 (reaction tier), R49. Deps: M5.1.
- **M6.2 Zulip adapter** — M. Hand-rolled sttp client (~250 lines): `register` with `event_types=[message, reaction]` and `narrow=[[is, dm]]`, long-poll `get_events` in a fork, re-register on `BAD_EVENT_QUEUE_ID`, edit-window probe at start, `REACTION_ALREADY_EXISTS` -> success, ignore own reactions, Zulip markdown, cursor `queue_id:last_event_id`, `vendor_event_id = zulip:{queue}:{id}`, 200 req/min bucket. Acceptance: suite A green against a Zulip fake wire; zero core files; live smoke on `docker-zulip`. Covers R58, R60 (zulip ids). Deps: M6.1.
- **M6.3-alt Out-of-process adapter host (conditional on an M6.0 no-go)** — M. A JSON-RPC host in `app/` over a local Unix socket speaking the `ChatAdapter` contract (serialisable since M0.9), a client stub in `core/chat` implementing the trait behind the existing interface, process supervision and restart under the Ox root scope, suite A run through the socket with the console adapter as the remote. Acceptance: suite A green through the socket; killing the remote process is recovered without losing an inbound event (cursor resume). Deps: M6.0 (no-go verdict only).
- **M6.3 Matrix adapter** — L (intermediate green: sends and reactions, then invites and the divergence check). Per the M6.0 verdict, in process or over M6.3-alt: direct-room invites accepted with allowlist, per-sender rate limit and leave after 10 min; `txnId = sendKey` sends, `sendKey:r{i}` reactions (`M_DUPLICATE_ANNOTATION` -> success), `m.replace` edits, `formatted_body` HTML, `vendor_event_id = matrix:{event_id}`, startup divergence check between the Trixnity store and the last inbound event, polls off, device verification documented; backup job extended if SQLite. Acceptance: suite A green; E2EE smoke in CI against Synapse; restore drill covers the Matrix store; an 8-dose digest is one message plus one reaction set within Synapse's burst. Covers R58, R60 (matrix ids). Deps: M6.0, M6.1 (and M6.3-alt on no-go).
- **M6.4\* Reaction-tier smoke and docs** — S. Live checklists and setup guides for Zulip and Matrix; one reaction-tier vendor configured as the owner's fallback. Acceptance: doc-lint green; each setup guide followed end to end in a recorded smoke from a fresh account to a resolved reminder. Deps: M6.2, M6.3.

**Exit criteria.** All four adapters pass suites A, B1 and B2 in CI; the spike document exists with its verdict; M6.4 `in-use` (one reaction-tier vendor as a fallback for seven days). **Non-goals.** Matrix polls, Zulip widgets, group rooms and streams.

### M7 — Breadth to v1.0

**Goal.** Every schedule kind in the spec, habits and mood as proper satellites, generic reminders with a UI, free-text logging, the read-only API.

- **M7.1 Every-N, cycle, interval kinds, additional slot groups** — M. `EveryNDays`, `EveryNWeeks`, `Cycle` with `anchorDate` inside the rule, `IntervalFixedStart` per local day with the 23 h/25 h notice; an "additional slot group" wizard branch so a Mon-Fri 08:00 / Sat-Sun 10:00 schedule is one `FixedTimes` rule; wizard branches for the new kinds; seven-zone goldens extended; personas enabled. Acceptance: every-8h across the transition days yields 3 and 4 doses; cycle phase unchanged across a pause; a Mon-Fri 08:00 / Sat-Sun 10:00 schedule materialises the right instant on each day in the seven-zone goldens. Covers R12, R20 (every N, cycle, weekend times via slot groups). Deps: M3.6.
- **M7.2 Chains and taper** — L (intermediate green: chain rows of `decide`, then taper and personas). `Chain(FirstTaken|EachTaken)` with children on terminal transitions, `chain_not_started` at cutoff, undo cascade, chain-bounded snooze; `Taper` phases; all eight personas in the simulation. Acceptance: table tests for chain rows of `decide`; undo of an anchor cancels children in one transaction. Covers R12, R20 (taper). Deps: M7.1.
- **M7.3 Refill tracking** — M. Expand migration adding stock count and threshold to medications; decrement on taken; low-stock nudge through the M3.5 generic reminder kind; refill logged as an action. Acceptance: stock never negative; nudge exactly once per threshold crossing. Covers R20 (refill). Deps: M7.1, M3.5.
- **M7.4 Habits** — L (intermediate green: create and check-in, then streaks and stats). Create wizard with types boolean/count/duration/measurement/avoid, cadence daily/weekdays/N per week/N per month/every N days, streak policy flexible/strict/none, check-in statuses done/partial/planned_skip/missed with value and note, `unique lower(name)` find-or-create, nudges through the M3.5 kind, rest days, best streak, weekly consistency, 7/30/90-day trends, forgiving copy; Habits menu; `dosecord.habit.checkin_recorded.v1`. Acceptance: property — a streak never breaks on a rest day; a check-in on an unknown habit offers creation instead of inserting a duplicate; stats golden. Covers R39, R40, R41, R42 (copy, rest days), C12, C14 (habit stats). Deps: M3.5.
- **M7.5 Mood breadth and NLU router** — M. Free-text mood, "I took <name>" and habit phrases through one router in `core/application`: word-boundary tokens, intent priority (intake before mood), multi-word medication names by `name_norm` prefix, negation, numeric extraction only in mood context; 7/30-day mood trends in `/stats`. Acceptance: regression tests for every C19 bug ("not bad", "didn't", "bread", "feel 100%"); "I took my pills and feel fine" is an intake; property tests over generated sentences. Covers R31 (mood, habits), R36, R52, C19. Deps: M7.4.
- **M7.6 Generic reminders UI** — M. Reminder create wizard (title, first time, repeat daily/weekly/none), the Reminders menu, Done/Snooze controls over the M3.5 kind, same delivery guarantees. Acceptance: exactly one Done-able message per `next_due_at` across restarts (`send_key`); snooze options are bounded by the next due; Done writes one action row with an `idempotency_key`; transcript golden on the console and Discord profiles. Covers R28. Deps: M3.5.
- **M7.7 Read-only REST API** — M. Expand migration adding `api_tokens` (hashed, scoped); Tapir `GET /api/v1/me/stats`, `/mood?days=`, `/medications/schedule` with bearer tokens issued and revoked from Account -> API tokens, 400/401/429 conventions, 10 req/min bucket, OpenAPI; `vendor_event_id = api:request:{id}` for API-originated intents. First candidate to move to DEFERRED at the M7 re-plan. Acceptance: OpenAPI contract test; 401 on a missing or bad token; 429 after 10 req/min; `/me/stats` equals the M3.4 query on a seeded fixture; a revoked token yields 401. Covers R34, R58 (API client), R60 (api ids). Deps: M4.1, M3.4.
- **M7.8 Native-image experiment** — S, spike. Mill `NativeImageModule` with JDA/Netty/Ktor reachability metadata. Go: the image runs the conformance suite; no-go acceptable, the JRE image with AppCDS stays. Note written to `docs/spikes/native-image.md`. Deps: M6.3.
- **M7.9 Documentation cut 3 and v1.0** — S. Vendor setup guides, operator runbook, ADR addenda from all spikes, CHANGELOG, tag. Acceptance: doc-lint green; the runbook's restore and unquarantine procedures followed end to end in a recorded drill; every spike note is linked from the ADR it amends. Covers R85. Deps: all of M7.

**Exit criteria.** The v1.0 Definition of Done (section 9). **Non-goals.** Section 7.

## 5. Vendor adapter track

Every adapter is a Mill module depending only on `contracts` and `core`, implements `ChatAdapter`, declares a static `CapabilityProfile`, maps errors into the closed `ChatError` hierarchy, passes suite A against a fake wire, has its profile in suite B1 (renderer goldens, from M0.9) and B2 (mediator resolution, as its tier lands), mints its own `vendor_event_id` scheme, ships with a live smoke transcript and a setup guide, and (from M5.1) passes the zero-core-files CI diff gate. "Supported" means the suites pass — nothing else. Discord is the first adapter to pass, not a special case.

| Vendor | Milestone | Spike / go-no-go | Capability degradation plan |
|---|---|---|---|
| Console | M0.12c | none | Text only, numbered options, `#<n>` target syntax, `CONSOLE_USER_ID`; the permanent boundary proof and the second channel for fallback and link tests |
| Discord (JDA 6.6) | M2.1-M2.3 (M2.1-M2.2 alongside M1) | Spike C (M0.8): user-install DM without shared guild; `enforce_nonce` fixture; modal on an un-acked interaction; autocomplete | Native buttons/select/modal/ephemeral; edit/delete any age; 5x5 rows, digests capped at 8 doses; two-row reminder; slash options with autocomplete and choices; ephemeral downgrades to `deleteAfter` without a live interaction; slash with `USER_INSTALL`, bot-DM context only; no message-content intent |
| Telegram (TelegramBots 10.3) | M5.1 (alongside M3) | Spike A (M0.6): 49-char token inside 64 bytes; conformance "toast after auto-answer" | Inline keyboard <= 4 per row; select -> paged buttons; modal -> `FormRunner` with `ForceReply`; ephemeral -> `deleteAfter 60 s` inside 48 h else notice; single-shot `answerCallbackQuery`, later toasts become 30 s messages or fold into the edited reminder; DM only after `/start` |
| Zulip (hand-rolled) | M6.2 | Edit-window probe at start; `BAD_EVENT_QUEUE_ID` conformance case | No buttons -> numbered legend plus <= 3 reactions (Taken, default Snooze, Skip) with a `choice_map` row; other snooze durations, notes and corrections via numbered replies; multi-select via space/comma numbers; Form -> `FormRunner`; ephemeral -> notice "not kept"; finalize = remove own reactions + reply-quote keeping `[Undo][Correct]` as a fresh reaction set |
| Matrix (Trixnity 5.8, Kotlin shim) | M6.0, M6.3 (M6.3-alt on no-go) | Spike D (M6.0): build under Ox, E2EE, repository; escape hatch: out-of-process adapter sized as M6.3-alt | As Zulip; `m.replace` edits; `txnId = sendKey`; invite-based DM; polls off; ephemeral -> redaction after 60 s; `eventsPerMessageBudget = 4` for Synapse's burst of 10; E2EE from the first slice |

## 6. Cross-cutting tracks

**Reliability and observability.** M0.3 constraints and injected `Clock`; M0.10 proves the dispatcher protocol before the loop exists; M0.11 masked JSON logs with correlation context; M1.6-M1.8 loop invariants (one row per `send_key`, savepoints, quarantine, heartbeats, epoch fencing), catch-up and `unknown`; M1.11 simulation as the review artefact for every loop change; M2.4 readiness, core metrics, rate-limited silent owner alerts, backups, restore drill and the operator CLI for quarantined and dead rows; M3.6 nightly fold-vs-projection and tz verify passes; M4.5 OTel traces and dashboards; M5.2 channel fallback. Alerts load-bearing from M2: tick age > 60 s, outbox dead > 0, materialised lag > 0, quarantined > 0, adapter disconnected > 5 min.

**Security and privacy.** M0.12a principal stamping (no account id from the wire), bad-MAC audit rows and visible failure when the database is down; M0.6 HMAC tokens with two keys; M0.3 INSERT-only audit tables with the erasure-role exception; M0.11 log masking; M2.1 DM-only enforcement and no privileged intent; M3.3 discreet mode; M4.1 auth link with CSRF, rate limits, generic errors and auth metrics; M4.2 Argon2id, recovery codes, unlink, lockout; M4.3 append-only audit and key rotation; M4.4 export and erasure; M4.6 security review and dependency audit before cross-vendor delivery; M6.3 E2EE on Matrix. Never: passwords in chat, clinical advice, medication names in logs above DEBUG; secrets via Docker secrets.

**Documentation consolidation.** Three cuts. M0.0 (first slice) checks in DESIGN.md, the ADRs, the design tracks, this roadmap and `docs/spikes/`; deletes `ARCHITECTURE.md`; archives `SWARM_RESEARCH_SUMMARY.md`; tombstones the Kafka sections of `PRODUCTION_PLAN.md`; reduces `API.md` to the M7.7 contract; stubs README, QUICKSTART and DEVELOPMENT; writes the doc-lint list that M0.4 enforces. M2.5 rewrites README, QUICKSTART and DEVELOPMENT for the real system, appends ADR-012 to `MEDICATION_REMINDER_UX.md` (which stays the spec), and settles the contradictions in writing. M7.9 adds vendor guides and the runbook. Every spike writes a note; every changed decision is an ADR addendum. This ROADMAP is updated at every milestone exit and Appendix B's status column is the only status source; a slice not in this file needs a roadmap PR first.

**Quality gates.** M0.1 `-Werror -Wunused:all`, scalafmt, scalafix; M0.4 CI (compile, tests with Postgres 18, `flyway validate`, vendor-symbol grep, licence gate, doc-lint) and M0.5 docker build; munit-scalacheck properties in M0.6, M0.7, M0.9, M1.2-M1.5, M1.7, M3.2, M7.5; Testcontainers Postgres in every infra slice; renderer goldens per profile (suite B1) from M0.9; adapter conformance suite A and mediator suite B2 from M0.13 with the zero-core-files diff gate from M5.1; scenario simulation from M1.11; coverage gate 85% on `core/domain` and `core/chat` armed at M1 exit, none on adapters; the README walkthrough job from M2.5; the dependency audit from M4.6.

## 7. Deferred and dropped

Deferred (kept as requirements, with a re-entry condition):

- **Sharing and caregiver consent (R43)** — needs a consent model and a second-party identity; re-enter after v1.0 with a spec.
- **Achievements (R42 part, `achievement.unlocked.v1`)** — forgiving copy and rest days ship in M7.4; gamification waits for real habit data.
- **Mood/medication/habit correlation hints (C14 part)** — re-enter after 90 days of real data on all three satellites.
- **Natural-language schedule parsing (C15)** — the wizard is the reliable path; re-enter when a corpus of real phrasing exists.
- **Email magic links (C16 part)** — no email column, no mail transport; the link challenge and recovery codes cover restore; re-enter with a verified-email feature.
- **Breached-password screening (C16 part)** — the auth path of a self-hosted, single-tenant deployment makes no outbound calls, and a fail-open k-anonymity check adds a network dependency to the one page that must always load; re-enter if the deployment becomes multi-tenant or an offline breached-hash list is packaged.
- **Integration service, external health apps, Redis, pgBouncer, load-balanced bot instances (C14 tail)** — single-tenant scale needs none; the role split is the scaling story; re-enter when metrics show a bottleneck.
- **REST API (R34)** — in M7.7 scope but the first slice to defer at the M7 re-plan if v1.0 is at risk; re-enter when a web or mobile client is planned.
- **Matrix polls (MSC3381), Telegram Mini Apps, Zulip widgets, Discord V2 components** — not part of the unified model in v1.
- **`adherence_daily` rollup** — only if 7/30-day aggregates become slow.
- **External relay of `domain_events` to NATS/Kafka** — behind an `EventBus` port when a consumer exists (ADR-003).
- **Native image (M7.8)** — experiment only, never a release dependency.

Dropped (no re-entry):

- **Kafka, ZooKeeper, Kafka UI, topics, offsets, partition keys, headers, DLQ topic, outbox-publisher process (R57, R62, R63, R66 as a topic, R77, C7, C8, K10, K11)** — ADR-003; intent lives in `inbound_events`, `outbox_messages` (`dead`), `domain_events`, the keyed executor and one transaction per event.
- **Retired scripting runtime, legacy migration tool, async SQL layer, consumer, and format/type-check tooling (R69, R70, R71, R73, R84, R82)** — ADR-001/002; replaced by Mill, Flyway, JDBC on virtual threads, scalafmt/scalafix.
- **DM password fallback (R7) and a chat `login` command (C4 part)** — ADR-007; no secret transits a chat vendor.
- **Discord Rich Presence (R54)** — a guild-oriented cosmetic feature contradicting a DM-only, discreet product; a static status remains.
- **Prefix text commands and the message-content intent (R53 as prefix, C23, K13)** — slash commands only.
- **Two-loop reminder/missed workers (R22/R23 as two processes)** — one loop, one column (ADR-004).
- **`--adapter` CLI flag** — adapters come from `ENABLED_ADAPTERS` (DESIGN.md sections 3 and 12).
- **Handle and display name at signup** — asked lazily (M0.12d, M4.1); K8's rule (never the vendor nickname) still holds.
- **`models.py` shim, duplicated producer wrappers, unreachable guards (C10, C22)** — deleted in M0.2.

## 8. Risk register

| # | Risk | Spike / mitigation | Go / no-go | Fallback |
|---|---|---|---|---|
| 1 | The 34-byte token exceeds Telegram's 64 bytes or fails on Discord | M0.6 properties | All shape, budget, round-trip and tamper properties pass | 4-byte inline value, slot mode for anything larger, ADR-006 addendum |
| 2 | DST resolution disagrees with the gap/fold policy or the JDK tzdb is stale | M0.7; goldens generated from `ZoneRules`; M3.6 nightly verify; tzdb metric | Seven-zone goldens pass on tzdb >= 2026c; nightly pass rewrites zero rows on an unchanged JDK | tzdata override in the image; startup assertion; 48 h horizon limits blast radius |
| 3 | JDA cannot DM from a user-installed app without a shared guild, or `enforce_nonce` is not sent | Spike C (M0.8); fixture on the JSON body (M2.2) | DM and proactive reminder arrive with no shared guild; fixture green | Partial: keep the home-guild step, `canInitiateDm = SharedGuildOrInstall`; no-go: reopen ADR-001's Discord-library lens before M2 |
| 4 | Dispatcher double-sends or double-applies under crash and concurrency | M0.10; M1.7 crash injection; `possible_duplicate` metric | One row per `send_key`; <= 1 flagged duplicate per crash; no extra state change | Redesign the claim/record protocol before M1.7 |
| 5 | Mill `kotlinlib` cannot build the Trixnity shim, `runBlocking` pins under Ox, or E2EE/repository fails | Spike D (M6.0), time-boxed; `ChatAdapter` kept JSON-serialisable since M0.9; M6.3-alt sized | E2EE round trip in CI; no leak over 1,000 cancellations; one repository option works | Partial: SQLite volume in the backup job; no-go: M6.3-alt (ADR-010) |
| 6 | Discord 3 s deadline missed under load or for `opensForm` | Watchdog at 1.5 s from `createdAt`, off only for `opensForm`; counted `FormRunner` fallback; ack-latency metric | p95 < 1.5 s on the smoke; `form_degraded_total` stays 0 | Cache the principal lookup; FormRunner |
| 7 | Telegram callback queries expire (~10 s, unpublished) before the answer | Pre-transaction toasts; 2 s watchdog; conformance "toast after auto-answer" (M5.1) | `ack_latency` p99 < 2 s on the smoke | Move the toast path entirely to the watchdog |
| 8 | Bespoke ~300-line scheduler bugs (revisions, catch-up, chains) | M1.2-M1.8 properties and infra scenarios; M1.11 simulation goldens; 85% coverage gate | Golden transcripts stable; two-loop and crash tests green | Quarantine and alerts; `dosecord admin occurrence` tools from M2.4 |
| 9 | The materialiser stops silently | Loop safety net; `min(materialized_through) < now` alert; `/readyz` (M2.4) | The alert fires in a test that disables the materialiser | Owner DM alert |
| 10 | Zulip realm edit window and reaction limits vary; hand-rolled client drift | Probe at start; conformance capability honesty; `BAD_EVENT_QUEUE_ID` case; docker-zulip smoke (M6.2) | Re-register exercised; edit window probed | Reaction tier degrades to numbered list only |
| 11 | Synapse per-account rate limit breaks reaction-tier reminders | `eventsPerMessageBudget = 4`; documented `rc_message` override (M6.3) | An 8-dose digest is one message plus one reaction set | Numbered list only |
| 12 | Virtual-thread pinning in JDBC or JDA | JDK 25 (JEP 491); Hikari sized for VTs; Ox `timeout` per vendor call | M1.11 load run with 50 concurrent chats shows no carrier starvation | Role split isolates the loop |
| 13 | JVM RSS above the 256 MB budget | Asserted <= 200 MB idle in M0.5; measured with Discord plus loop in M2.4; SerialGC, AppCDS | RSS under 300 MB with Discord plus loop | M7.8 native-image experiment |
| 14 | ADR-008 timing deviation (deletion in M0.2, not the Discord PR) | Gap-inventory evidence (R70, R86) that no working path exists | Amendment recorded in ADR-008 at M0.2 | None needed |
| 15 | Single-developer bandwidth: M0 is wide, L slices stall | Risk-first slices are S/M and interleave; every L names an intermediate green state; strict order; the Discord and Telegram adapters interleave with M1 and M3 so no milestone is a single serial chain | An overrun spike is recorded "no answer yet" and the dependent milestone marked `blocked`, never assumed | A slice open longer than two milestones is split at milestone exit |
| 16 | Documentation entropy returns; scope creep from five legacy docs | M0.0 deletes, archives and stubs first; doc-lint CI from M0.4; changes only via ADR addenda; section 7 list | Doc-lint green; every slice traces to this file | A roadmap PR precedes any unlisted slice |
| 17 | Medical-adjacent data correctness (wrong adherence) | Adherence invariants as properties (M1.4b); `unknown` excluded; nightly fold check (M3.6); corrections in the daily-driver scope (M1.10) | Fold mismatch alerts | Corrections flow repairs outage gaps |
| 18 | Ox 1.x / Mill 1.x / JDA 6.x / TelegramBots 10.x API churn | Pinned versions; upgrade PRs run the full suite | CI green | Pin back |
| 19 | The owner answers reminders outside the chat (console, `/taken`) during the in-use week, hiding UX defects | DoD counts `dose_actions` by `interaction_id` versus command; no console use during the week | Every scheduled dose resolved from the reminder message or Today | Extend the in-use week; fix the tap budget before tagging |

## 9. Definition of Done

**Daily-driver release (end of M2, tagged `0.1.0`).** The owner's real regimen (fixed-times and as-needed medications) runs on Discord for seven consecutive days: every scheduled dose produced exactly one reminder at the correct local time (verified against `dose_actions`), zero duplicate reminders, zero `unknown` rows outside deliberate tests, Taken/Snooze/Skip/Undo work on the live message and Taken on a message older than a day opens the correction and records `manually_corrected`, every scheduled dose in the seven days was resolved from the reminder message or Today (counted by `interaction_id` on `dose_actions`, none by console), the reminder push was received on Discord mobile within 10 s of due, one wizard was completed on mobile without typing an IANA zone, the owner reviewed yesterday's doses with `/history` from the Discord DM, one as-needed dose was logged in two taps, a restart and a deliberate 1 h outage produce one digest and no stale reminders, `/today` matches the database, every user action has a `dose_actions` row with an idempotency key, alerts reach the owner's DM silently and no more than once per condition per hour, `/readyz` reflects the real state, nightly backups run and one restore drill passed, the M0.6, M0.7, M0.8 and M0.10 notes have written verdicts, suite A is green for Discord, suite B1 for all five profiles and suite B2 for console and discord, the 30-day simulation goldens are committed, the two-loop, poison-row, crash-injection, 6 h jump and pause/resume infra tests are green, CI enforces `-Werror`, the 85% coverage gate, the vendor-symbol grep, Flyway validate and doc-lint, no Python, Kafka or Poetry file exists, and README, QUICKSTART, DEVELOPMENT, DESIGN, the ADRs and this ROADMAP are the only architecture documents.

**v1.0 (end of M7).** Everything above plus: all four adapters pass suites A, B1 and B2 in CI with recorded live smokes; Telegram, Zulip and Matrix were delivered with zero core files changed; the owner has used at least two vendors with fallback; the Matrix spike document exists with its verdict and the E2EE smoke runs in CI; every `Rule` kind is materialised, golden-tested in seven zones and present in the eight-persona simulation; notes, custom-time corrections, revisions, quiet hours, discreet mode, stats, paged history, weekly summary, habits, mood trends, free-text logging, generic reminders, refill tracking and the read-only API (or its recorded deferral) are `green`; auth link, Argon2id, recovery codes, unlink, audit log, key rotation, export and erasure are `green` and passed the security review; tracing and dashboards are in place; the nightly fold-vs-projection and tz verify passes have run clean for thirty days; vendor guides, the runbook and CHANGELOG are published; Appendix B shows no `todo` and Appendix A no id outside `green`, DEFERRED or DROPPED.

## 10. Appendix A — Requirement traceability

Every R-id, every critic omission (C) and every critic correction (K) maps to a primary slice (the one that makes it `green`), secondary slices after a semicolon, or DEFERRED/DROPPED with the reason in section 7.

| Id | Requirement (short) | Milestone / slice or disposition |
|---|---|---|
| R1 | Canonical account vs platform identities | M0.12d; schema M0.3 |
| R2 | /start create/link/restore | M0.12d (create); M4.2 (full menu on every vendor); M5.3 (second vendor); M4.1 (link flow) |
| R3 | Signup creates and links; rejections | M0.12d (create, already-linked rejection); M4.1 (taken handle at the handle step) |
| R4 | Link existing account | M4.1; M5.3; M2.4 (operator bridge) |
| R5 | Restore via recovery codes | M4.2 |
| R6 | Challenge, Argon2id, recovery, audit, rate limits | M4.1; M4.2; M4.3; M4.6 |
| R7 | DM password fallback | DROPPED (ADR-007) |
| R8 | Account id never from the wire | M0.12a |
| R9 | Persisted conversation state with expiry | M0.12b; schema M0.3 |
| R10 | Timezone capture, confirm, change | M0.12d (picker and echo); M1.9 (wizard confirm card, Account -> Timezone); M1.5 (tz-follow data); M3.2 (move questions) |
| R11 | Medication profile incl. instructions | M1.9 (wizard form); data M1.5; edit M3.2 |
| R12 | Schedule kinds | M1.1 (enum); M1.2, M1.9 (fixed, as-needed); M7.1; M7.2 |
| R13 | Multiple times, selected weekdays | M1.2; M1.9 |
| R14 | As-needed manual logging | M1.10 (`/log`, Medications -> Log dose, Today `[Log <name>]`); M1.2; M7.5 ("I took <name>") |
| R15 | Medication commands | M1.9 (create/pause/resume/archive/today/history); M3.2 (update, history paging); data M1.5 |
| R16 | Reminder policy | M1.1; M1.9 (exposed under Medications -> Settings); M3.3 (discreet) |
| R17 | Guided wizard | M1.9; M2.2 (modal step) |
| R18 | One medication per name | M0.3 (index); M1.5; M1.9; M3.2 |
| R19 | Quiet hours | M1.1, M1.3 (semantics); M3.3 |
| R20 | Next-iteration kinds, weekend times, refill | M7.1 (every N, cycle, slot groups); M7.2 (taper); M7.3 (refill) |
| R21 | First-class occurrences | M0.3; M1.2; M1.5 |
| R22 | Reminder worker | M1.6; M1.3; M1.8 |
| R23 | Missed-dose handling and notice | M1.3; M1.6; M1.10 (notice with [I took it][Skip][Keep missed]) |
| R24 | Reminder with one-tap actions, follow-ups | M2.2 (buttons, [Undo][Correct]); M1.10 (numbered); M3.1 ([Add note]); M6.1 (reactions) |
| R25 | Mark taken, scheduled vs taken | M1.10; M1.3; M3.1 (custom-time correction) |
| R26 | Skip reasons, snooze, correct, undo | M1.10 (snooze, undo, correct Now/Scheduled); M1.3; M3.1 (reasons, custom) |
| R27 | Dose action audit log | M0.3; M1.10; M3.1 |
| R28 | Generic reminders | M7.6 (UI); M3.5 (kind in the loop) |
| R29 | UTC storage, tz for display/recurrence | M0.3; M0.7; M1.2; M3.2 |
| R30 | Domain events | M0.12a (rows, source, account_id); M0.12d (account_created, checkin_recorded); M1.5 (schedule_created); M1.6 (dose_due); M1.10 (intake_taken); M3.4 (subscriber); M7.4 (habit) |
| R31 | /stats | M3.4 (medication); M1.4b (maths); M7.4 (habits); M7.5 (mood) |
| R32 | Adherence definition | M1.4b; M1.8; M3.4 |
| R33 | Weekly summary | M3.5 |
| R34 | Read-only REST API | M7.7 (first candidate to DEFER at re-plan) |
| R35 | /mood command | M0.12d; M2.1 (integer choices) |
| R36 | Free-text mood | M7.5 |
| R37 | Mood note and tags | M0.12d (`#tag` syntax into `tags text[]`) |
| R38 | Mood vertical slice | M0.12d |
| R39 | Habit check-in and streak | M7.4; constraint M0.3 |
| R40 | Habit creation taxonomy | M7.4 |
| R41 | Check-in statuses | M7.4 |
| R42 | Forgiving habit UX, achievements | M7.4 (copy, rest days); achievements DEFERRED |
| R43 | Sharing and caregiver consent | DEFERRED |
| R44 | Export and delete | M4.4 (erasure role) |
| R45 | DM-only, discreet | M2.1; M3.3; M5.2 |
| R46 | Non-clinical, non-shaming copy | M1.4a |
| R47 | All interaction in DMs (K1 applied) | M2.1 |
| R48 | Menu tree | M1.9; labels M1.4a |
| R49 | Backend-driven UI contracts | M0.9; M0.6; M2.2; M6.1 |
| R50 | Bot renders core replies | M0.12a (mediator delivers), M0.12c (console renders); superseded by the in-process mediator |
| R51 | /help and welcome | M0.12d |
| R52 | Free-text medicine/habit logging | M7.5 |
| R53 | Command interaction model | M2.1 (slash only, autocomplete; prefix DROPPED) |
| R54 | Rich Presence | DROPPED |
| R55 | CloudEvents envelope | M0.9 |
| R56 | Adapters emit only intents, core emits facts | M0.9; M2.2; M1.7 |
| R57 | Partition-key flip | DROPPED; per-chat ordering M0.12a |
| R58 | Further vendors reuse contracts and account | M0.12c (first adapter); M5.1; M5.2; M6.2; M6.3; M7.7; gate M0.13, M2.3 |
| R59 | Single contracts package | M0.1; M0.9 |
| R60 | Per-platform idempotency keys | M0.12a (`inbound_events` PK); M2.1 (discord); M5.1 (telegram); M6.2 (zulip); M6.3 (matrix); M7.7 (api) |
| R61 | Versioned message types | M0.9 |
| R62 | Kafka topics | DROPPED; tables M0.3 |
| R63 | Manual offset commit | DROPPED; one transaction per event M0.12a |
| R64 | Processed-message idempotency | M0.12a (`inbound_events` with replay) |
| R65 | Transactional outbox with SKIP LOCKED | M1.7; proof M0.10; table M0.3 |
| R66 | DLQ | M1.7 (`dead`); M2.4 (operator CLI); topic DROPPED |
| R67 | Retry and backoff | M1.7 (property); M0.10; M1.8 |
| R68 | At-least-once from adapters, visible failure | M1.7; M0.10; M0.12a and M2.3 (failure toast with the pool paused) |
| R69 | Legacy migrations | M0.3 (Flyway); legacy tool DROPPED |
| R70 | Async SQLAlchemy/asyncpg | DROPPED; JDBC M0.3 |
| R71 | Non-blocking consumer | DROPPED; virtual threads M0.5 |
| R72 | Separate entrypoints | M0.5; M4.3 (`--role`) |
| R73 | Poetry scripts | DROPPED; `dosecord` CLI M0.5 |
| R74 | Structured logs with correlation ids | M0.11 |
| R75 | OpenTelemetry tracing | M4.5 |
| R76 | Metric set incl. auth failures and rate-limit hits | M2.4 (core); M4.1 (auth counters); M4.5 (full) |
| R77 | Kafka UI | DROPPED (M0.2) |
| R78 | Docker Compose infra | M0.2; M0.5; M2.4; M4.5 |
| R79 | Reproducible builds | M0.1 |
| R80 | Layered tests | M0.4; M0.13; M1.11; M3.6 |
| R81 | CI pipeline | M0.4 |
| R82 | Lint/format/type gates | M0.1 (scalafmt, scalafix, -Werror); Python tooling DROPPED |
| R83 | Configuration and env vars | M0.5 |
| R84 | Python 3.10 runtime | DROPPED (ADR-001) |
| R85 | Documentation currency | M0.0; M0.4 (doc-lint); M2.5; M7.9 |
| R86 | Exactly one execution per message | M0.12a; M2.3 |
| C1 | `reply_to` ignored | M0.9, M0.12a (in-process reply) |
| C2 | Resolved account not propagated | M0.12a (account_id on every reply and event, asserted) |
| C3 | Duplicates get no reply | M0.12a (replayed reply) |
| C4 | login / platform_linked / unlink | M4.1 (`platform_linked`); M4.2 (unlink); login DROPPED |
| C5 | conversation_id on sessions | M0.3; M0.12b (session id, `step_seq`) |
| C6 | Envelope source naming | M0.9 (property on `source`) |
| C7 | Kafka header asymmetry | DROPPED; correlation on rows M0.11, M0.12a |
| C8 | Topic provisioning, events config | DROPPED; `domain_events` M0.3 |
| C9 | Service layout | M0.1 |
| C10 | README claims, duplicated producers | M0.0; M0.2; M2.5 |
| C11 | DB enums and CHECK constraints | M0.3 |
| C12 | Avoid/Amount habit types | M7.4 |
| C13 | Bounded repeat-reminder cadence | M1.3; M1.6 |
| C14 | Habit stats; correlation hints; Redis, pgBouncer, LB, integrations | M7.4 (stats, 90-day trends); correlation hints DEFERRED; infrastructure DEFERRED |
| C15 | NL schedule parsing | DEFERRED |
| C16 | Password policy, breach screening, magic links | M4.2 (min length, rehash, lockout); breach screening DEFERRED (own reason); email links DEFERRED |
| C17 | Contract-level validation | M0.9; M1.1 |
| C18 | /mood junk notes | M0.12d |
| C19 | Free-text router bugs | M7.5 |
| C20 | Shutdown and signal handling | M0.5 |
| C21 | Lint/test coverage of all modules | M0.4 |
| C22 | Dead code surfaces | M0.2 |
| C23 | '/' prefix collision, message-content intent | M2.1; facts M0.8 |
| K1 | R47 contradicted, must actually hold | M2.1 |
| K2 | R57 account branch has no writer | M0.12a (per-chat ordering replaces it) |
| K3 | "implemented" means "code exists" | Section 1 status vocabulary; Appendix B |
| K4 | Line-number corrections | No work |
| K5 | File count is 36 | Section 2; no work |
| K6 | Env-file rejection condition | M0.5 (generated `.env.example`) |
| K7 | Naive timestamps, insert-only `updated_at` on sessions | M0.3; M0.12b (`updated_at` asserted) |
| K8 | Display name leaked from vendor | M0.12d (neutral default, never the vendor nickname, asserted) |
| K9 | Help embed completeness | M0.12d, M1.9 (help from registry, includes itself) |
| K10 | `message_rejected` type comment-only | DROPPED with Kafka; `dead` status M1.7 |
| K11 | Producer flush split | DROPPED with Kafka; dispatcher accounting M1.7 |
| K12 | Habit taxonomy and reminder copy choices | M1.4a |
| K13 | message-content intent misleading | M2.1 |

## 11. Appendix B — Slice status

The only status source. Asterisk = `in-use` required before the milestone exits. Deps are the strict-order dependencies of section 4.

| Slice | Name | Size | Deps | In-use | Status |
|---|---|---|---|---|---|
| M0.0 | Documentation cut 1 | S | — | | green |
| M0.1 | Mill build and module graph | S | M0.0 | | green |
| M0.2 | Delete Python, Kafka, old compose | S | M0.1 | | green |
| M0.3 | Flyway V1, SQL layer, UnitOfWork, sessions, Testcontainers | M | M0.2 | | green |
| M0.4 | CI and quality gates, doc-lint | S | M0.3 | | green |
| M0.5 | Settings, root, health, image, shutdown, migrate | M | M0.4 | | green |
| M0.6 | CallbackCodec and action registry (kept) | S | M0.1 | | green |
| M0.7 | DST materialiser primitives (kept) | S | M0.1 | | green |
| M0.8 | Spike C: JDA DM (throwaway) | S | M0.1 | | todo |
| M0.9 | Contracts, chat model, renderer, FakeAdapter, goldens (B1) | M | M0.6 | | green |
| M0.10 | Outbox dispatcher protocol (kept) | M | M0.3, M0.9 | | green |
| M0.11 | Telemetry skeleton | S | M0.5 | | green |
| M0.12a | Mediator core on FakeAdapter | M | M0.5, M0.9, M0.11 | | green |
| M0.12b | WizardEngine, sessions, sweeper, FormRunner | M | M0.12a | | green |
| M0.12c | Console adapter | S | M0.12a | | green |
| M0.12d | Account create, /mood, /help | M | M0.12b, M0.12c | | green |
| M0.13 | Conformance suite v1 (A, B2 console) | M | M0.12d | | green |
| M1.1 | Rule enum, ReminderPolicy, QuietHours | S | M0.9 | | green |
| M1.2 | Evaluator and materialiser (pure) | M | M1.1, M0.7 | | green |
| M1.3 | Occurrence FSM decide() | L | M1.2 | | green |
| M1.4a | Copy catalogue and control layouts | S | M0.9 | | green |
| M1.4b | Projection fold and adherence | M | M1.3 | | green |
| M1.5 | Repositories, revisions, materialiser job, fixtures | L | M1.2, M0.3 | | green |
| M1.6 | ReminderLoop | L | M1.3, M1.5 | | green |
| M1.7 | Outbox dispatcher and delivery channels | L | M1.6, M0.10, M1.4a | | green |
| M1.8 | Catch-up and digest | M | M1.7 | | green |
| M1.9 | Wizard, menu, Today, History, Pause/Resume, Timezone | L | M1.5, M1.4a, M0.12d | | green |
| M1.10 | One-tap flows, correction, Log dose, commands | L | M1.7, M1.9, M1.4b | | green |
| M1.11 | 30-day simulation | M | M1.8, M1.10 | | green |
| M2.1 | JDA gateway, slash commands, ack policy | L | M0.13, M0.8, M0.12d (interleaves with M1) | | todo |
| M2.2 | Components, modals, ephemeral, idempotent sends | M | M2.1 | * | todo |
| M2.3 | Conformance gate, live smoke, tap budget | M | M2.2, M1.11 | * | todo |
| M2.4 | Readiness, metrics, alerts, backups, operator CLI | M | M2.3 | * | todo |
| M2.5 | Documentation cut 2 | M | M2.3 | | todo |
| M3.1 | Notes, skip reasons, custom-time corrections | M | M2.3 | * | todo |
| M3.2 | Schedule edit, tz-move questions, History paging | L | M3.1 | * | todo |
| M3.3 | Quiet hours and discreet mode | M | M2.3 | * | todo |
| M3.4 | Stats | M | M3.1, M1.4b | * | todo |
| M3.5 | Generic reminder kind, weekly summary | M | M3.4 | | todo |
| M3.6 | Simulation extended, nightly checks | S | M3.2, M3.3 | | todo |
| M4.1 | Tapir web edge, auth-link, handle step, auth metrics | L | M2.4, M0.12d | | todo |
| M4.2 | Credentials, recovery, restore, unlink | M | M4.1 | | todo |
| M4.3 | Audit, key rotation, roles, remaining admin CLI | S | M4.1 | | todo |
| M4.4 | Export and erasure | M | M4.1 | | todo |
| M4.5 | Tracing, full metrics, dashboards | M | M2.4, M4.1 | | todo |
| M4.6 | Security review gate, dependency audit | S | M4.2, M4.4 | | todo |
| M5.1 | TelegramBots adapter (zero core files) | L | M2.3 (interleaves with M3) | | todo |
| M5.2 | Multi-channel fallback | M | M5.1, M4.6 | * | todo |
| M5.3 | Cross-vendor link UX and smoke | S | M5.2, M4.1 | | todo |
| M6.0 | Spike D: Trixnity (throwaway) | M | M5.1 | | todo |
| M6.1 | Reaction tier in the mediator | M | M5.1 | | todo |
| M6.2 | Zulip adapter | M | M6.1 | | todo |
| M6.3-alt | Out-of-process adapter host (conditional) | M | M6.0 no-go | | todo |
| M6.3 | Matrix adapter | L | M6.0, M6.1 (M6.3-alt on no-go) | | todo |
| M6.4 | Reaction-tier smoke and docs | S | M6.2, M6.3 | * | todo |
| M7.1 | Every-N, cycle, interval, slot groups | M | M3.6 | | todo |
| M7.2 | Chains and taper | L | M7.1 | | todo |
| M7.3 | Refill tracking | M | M7.1, M3.5 | | todo |
| M7.4 | Habits | L | M3.5 | | todo |
| M7.5 | Mood breadth and NLU router | M | M7.4 | | todo |
| M7.6 | Generic reminders UI | M | M3.5 | | todo |
| M7.7 | Read-only REST API | M | M4.1, M3.4 | | todo |
| M7.8 | Native-image experiment | S | M6.3 | | todo |
| M7.9 | Documentation cut 3 and v1.0 | S | all of M7 | | todo |

## 12. Appendix C — Review disposition

Every finding of the three reviews (coverage, sequencing, experience) of the 2026-09-11 draft was accepted and applied, with the adaptations below where the fix differs from the one proposed. Nothing was rebutted.

- **M0.12 split** (coverage major, sequencing blocker): split into M0.12a-d as the sequencing review proposed (mediator; wizard engine; console module; first flows), each with its own acceptance list; `delivery_channels` insert on create and `choice_map` plus the digit gate are stated in M0.12a/d.
- **R60 / R30 attribution and acceptance** (coverage majors): R60 primary M0.12a with per-vendor secondaries; R30 primary M0.12a with one acceptance line per event in M0.12d, M1.5, M1.6, M1.10 and the subscriber test in M3.4.
- **Missing acceptance criteria** (coverage major and minors): M7.6, M7.7, M6.4, M7.9 now have pass/fail criteria; R3, R9, R37, K8, K7, C2, C6, R11, R14, R20 each have an asserting line; M0.5 RSS threshold, M1.7 backoff property, R68 failure-toast tests, M4.6 dependency audit added.
- **Documentation sequencing** (sequencing major): docs cut 1 is M0.0, doc-lint lands in M0.4 with an allowlist, M2.5 is the full rewrite; ADR-008's amendment is appended to a file that exists.
- **M1.4 orphan, spike code disposition, M0.10 deps, docker-build step, naming drift** (sequencing majors and minors): M1.4 split into M1.4a/M1.4b and wired into M1.7, M1.9, M1.10, M3.4; M0.6/M0.7/M0.10 relabelled as risk-first slices with kept code and M0.9 promotes the codec; M0.10 depends on M0.9 and the `ReadWriter` test moved to M0.9; docker build moved to M0.5; `ENABLED_ADAPTERS` replaces the `--adapter` flag; `migrate` lives in M0.5, `UnitOfWork` and the session repository in M0.3.
- **M6.1 narrowed and DoD suite B split** (sequencing majors): choice_map, quoted-reply numbers and the digit gate moved to M0.12a with the console `#<n>` syntax; suite B split into B1 (renderer goldens, all profiles, from M0.9) and B2 (resolution per profile as tiers land); the `0.1.0` DoD names B1 for all profiles and B2 for console and discord.
- **Telegram early** (sequencing major): M5.1 depends on M2.3 and interleaves with M3; M5.2/M5.3 stay behind M4.6; recorded in section 1.
- **Erasure vs append-only** (sequencing major): chose a GUC-gated `DELETE` under a separate erasure role, with the trigger clause in V1 (M0.3) and an ADR-002 addendum in M4.4, over anonymisation — `dose_snapshot` jsonb carries medication names, so anonymisation would need `UPDATE`s on the same append-only rows.
- **M3.5 resize** (coverage and sequencing): resized to M with the view, the loop's kind dispatch and a minimal non-dose FSM; added to M7.3's deps; `api_tokens` moved to an M7.7 expand migration; the M0 exit criterion names the enumerated V1 list; V1 is editable in place until `0.1.0`.
- **M1.9/M1.10 order** (sequencing minor): swapped so the wizard precedes the one-tap flows and the transcript can create its own schedule; a test fixture builder is also added in M1.5; the tap-waits test moved to M1.10.
- **Operator tooling, M4.1 console link, M6.3-alt, GUILD_INSTALL addendum, status column** (sequencing minors): outbox retry/dead and occurrence show/unquarantine moved to M2.4; `CONSOLE_USER_ID` added in M0.12c and the M4.1 link test runs FakeAdapter-to-FakeAdapter and Discord-to-console; M6.3-alt sized; M0.8 Go produces an ADR-001/ADR-013 addendum; Appendix B added with asterisks.
- **Correction in the daily driver** (experience blocker): the two-choice correction (Now / At scheduled time), the post-undo-window and beyond-late-log entry points are in M1.10 and M2.2; chip sets per milestone fixed (`[Undo][Correct]` at M2, `[Add note]` from M3.1); `dose.note` hidden by flag until M3.1; the M2.3 smoke names the expected reply.
- **Discord early** (experience major): M2.1 and M2.2 depend on M0 only and interleave with M1; M2.3 waits for M1.11. Adaptation: M0's exit cannot depend on an M2 slice, so the `in-use` week for `/start`, `/mood`, `/help` is attached to M2.2\* (running concurrently with M1) rather than to M0's exit; M0 exits on `green` plus a demonstration.
- **History, timezone, wizard shape, medication-targeted paths, reminder layout, reaction-tier gaps, Pause/Resume, hidden Link/Restore, console target syntax, ops alerts, DoD chat criteria** (experience majors and minors): all applied in M0.9, M0.12d, M1.4a, M1.9, M1.10, M2.1, M2.3, M2.4, M6.1 and section 9.
- **Lazy onboarding** (experience minor): accepted; adaptation: R3's "taken handle" rejection is tested where the handle is now chosen (M4.1), and the "already-linked identity" rejection stays in M0.12d.
- **R4 sequencing** (coverage minor): both proposed options combined — the Discord account is created fresh by default and console data is not carried, and `dosecord admin identity link` in M2.4 is the operator-only bridge (no secret transits chat, ADR-007).
- **Breached-password screening** (coverage minor): chose the "own reason and re-entry condition" option over adding a k-anonymity check (section 7).
- **Appendix A corrections** (coverage minor): R23, R24, R72, K10/K11, R2 rows corrected; C14 correlation hints listed as deferred; R76 gains M4.1.
